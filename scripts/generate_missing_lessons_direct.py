import os

def create_week_17_ru():
    title = "Продвинутый RAG для продакшна"
    
    lecture = """# Неделя 17. """ + title + """

Добро пожаловать на 17-ю неделю нашего профессионального курса для AI Automation Architects. На этой неделе мы погрузимся в одну из самых критически важных и востребованных тем в современной индустрии искусственного интеллекта — проектирование и развертывание продвинутых систем RAG (Retrieval-Augmented Generation) корпоративного уровня. 

В то время как базовый RAG отлично подходит для простых демонстрационных сценариев, он неизбежно терпит крах при встрече с реальной корпоративной спецификой: сложными многостраничными PDF-файлами, запутанными финансовыми таблицами, высокими требованиями к точности ответов (нулевая терпимость к галлюцинациям) и жесткими ограничениями по времени отклика (latency) и бюджетам на токены.

В ходе этой недели мы разберем продвинутые методы умного семантического чанкинга, реализуем двухуровневую архитектуру извлечения с помощью Parent-Document Retriever и Cohere Rerank, научимся переводить и декомпозировать сложные запросы пользователей, парсить сложные неструктурированные макеты документов и масштабировать векторные базы данных под миллионными нагрузками. Наконец, мы напишем полнофункционального агента Corrective RAG (CRAG) на чистом Python и внедрим двухэтапный контур контроля качества Evaluator-Optimizer для полной победы над галлюцинациями.

---

## Блок 1: Умный чанкинг — продвинутые стратегии нарезки текстов с умным перекрытием

Стандартный подход к подготовке данных для векторного поиска, заключающийся в разбиении текста на фиксированные отрезки (например, ровно по 500 символов с перекрытием 50 символов), является главной причиной сбоев RAG в продакшне. В реальных документах логические мысли, определения и таблицы распределены неравномерно. Когда граница чанка проходит посередине важного численного значения или разрывает логический абзац на две части, семантический вектор каждого из полученных фрагментов искажается. В результате при поиске система извлекает нерелевантный контекст, а модель на этапе генерации придумывает (галлюцинирует) недостающие факты.

### Продвинутые стратегии нарезки текстов

1. **Семантический чанкинг (Semantic Chunking)**
   Вместо того чтобы полагаться на фиксированное количество символов или слов, семантический чанкинг использует саму языковую модель (или модель эмбеддингов) для поиска логических границ. Процесс выглядит следующим образом:
   - Текст разбивается на отдельные предложения с помощью регулярных выражений или библиотек NLTK/Spacy.
   - Для каждого предложения генерируется векторное представление (embedding).
   - Вычисляется косинусное сходство (cosine similarity) между соседними предложениями.
   - Строится график перепадов семантического расстояния. Точки, где сходство резко падает (ниже определенного статистического порога, например, 95-го перцентиля отклонений), помечаются как естественные границы чанков.
   Это гарантирует, что каждый чанк представляет собой семантически законченную единицу информации.

2. **Структурно-ориентированный чанкинг (Structural Chunking)**
   Данный метод опирается на внутреннюю разметку документа (Markdown заголовки, теги HTML, структуру XML). Разбиение происходит строго по границам разделов (`#`, `##`, `###`), что позволяет сохранять иерархическую контекстную подчиненность. Например, при нарезке руководства пользователя каждый чанк будет четко соответствовать отдельной инструкции или подразделу, не смешиваясь с соседними.

3. **Макето-ориентированный чанкинг (Layout-Aware Chunking)**
   Применяется для документов в формате PDF или сканов. С помощью продвинутых библиотек (таких как PDF-Plumber, LayoutParser или специализированных OCR-сервисов) документ сначала сегментируется на визуальные зоны: основной текст, колонки, таблицы, врезки и сноски. Чанки формируются строго на основе этих физических блоков, исключая попадание колонтитулов или сквозных номеров страниц в содержательную часть вектора.

```ascii
========================================================================================
СЕМАНТИЧЕСКИЙ ЧАНКИНГ: ИДЕНТИФИКАЦИЯ ГРАНИЦ НА ОСНОВЕ РАССТОЯНИЯ ЭМБЕДДИНГОВ
========================================================================================

 Предложение 1 -----> [Embedding 1] 
                                    \\  Косинусное сходство: 0.94 (Высокое -> Объединяем)
 Предложение 2 -----> [Embedding 2] /
                                    \\  Косинусное сходство: 0.72 (Низкое -> ГРАНИЦА ЧАНКА!)
-------------------------- РАЗРЫВ ЧАНКА ------------------------------------------------
 Предложение 3 -----> [Embedding 3] /
========================================================================================
```

### Практические рекомендации по проектированию перекрытия (Overlap)
Перекрытие необходимо для сохранения непрерывности контекста на стыке чанков. В продвинутых системах размер перекрытия должен быть динамическим. Например, для семантического чанкинга перекрытие может составлять 1-2 предложения, гарантируя, что контекст предыдущей логической мысли плавно перетекает в следующую, предотвращая потерю связующих местоимений и ссылочных конструкций.

---

## Блок 2: Parent-Document Retriever — сохранение широкого контекста при точечном извлечении чанков

При проектировании базовых систем RAG разработчики неизбежно сталкиваются с фундаментальным противоречием между процессами извлечения информации (retrieval) и генерации ответа (synthesis):
- **Для эффективного поиска** векторной базе данных требуются максимально сжатые, чистые и тематически сфокусированные фрагменты текста (100-250 токенов). В мелких чанках отсутствует лишний "шум", их семантические векторы получаются более выраженными, что резко повышает точность семантического соответствия.
- **Для качественной генерации** языковой модели (LLM) необходим максимально широкий контекст (1000-2000 токенов). Модель должна видеть предысторию вопроса, логику рассуждения автора, оговорки и сопутствующие факты, иначе она построит сухой или логически неполный ответ.

Паттерн **Parent-Document Retriever** (Извлечение родительских документов) изящно разрешает это противоречие, разделяя индекс поиска и контекст генерации.

```ascii
========================================================================================
СХЕМА АРХИТЕКТУРЫ: PARENT-DOCUMENT RETRIEVER
========================================================================================

     [ База данных документов ] <----------------------------------------+
                 |                                                       |
                 | 1. Сохраняет полный документ (Parent)                 |
                 v                                                       |
     [ Документ: 2000 токенов ]                                          |
                 |                                                       |
                 | 2. Нарезает на дочерние чанки                         |
                 v                                                       |
        +--------+--------+                                              |
        |                 |                                              |
        v                 v                                              |
   [Child 1: 150t]   [Child 2: 150t] -----> [ Векторная база знаний ]    |
                                                     |                   |
                                                     | 3. Поиск находит  |
                                                     |    Child 2        |
                                                     v                   |
                                            [ Извлечение Родителя ] -----+
                                            (Передает в LLM 2000 токенов)
========================================================================================
```

### Принцип работы Parent-Document Retriever
1. Исходные документы сохраняются в реляционной или документоориентированной базе данных (например, PostgreSQL или MongoDB) в виде крупных структур — это **Parent Documents** (Родительские документы). Каждому родителю присваивается уникальный идентификатор `parent_id`.
2. Каждый родительский документ нарезается на множество мелких дочерних фрагментов — **Child Chunks** (обычно по 100-150 токенов).
3. Для каждого дочернего чанка генерируется эмбеддинг, который сохраняется в векторную базу данных вместе с метаданными, содержащими ссылку на `parent_id`.
4. В момент запроса пользователя система выполняет семантический поиск строго по дочерним чанкам в векторной базе.
5. Найдя наиболее релевантный дочерний чанк, система извлекает из метаданных `parent_id`, обращается к основной базе документов, достает **целиком весь родительский документ** (или заданное широкое окно вокруг дочернего чанка) и передает его в промпт для LLM.
Это позволяет осуществлять точечный семантический поиск без потери глубины и объема контекста на этапе генерации.

---

## Блок 3: Переранжирование (Reranking) — интеграция API Cohere Rerank для фильтрации результатов векторного поиска

Базовый семантический поиск (векторный поиск по косинусному сходству) оптимизирован для выполнения быстрых математических операций над миллионами векторов. Однако он обладает существенным недостатком: модели эмбеддингов сжимают весь смысл текста в один вектор фиксированной размерности (например, 1536 значений), из-за чего теряются мелкие детали, логические отрицания и тонкие синтаксические связи. В результате векторный поиск часто возвращает документы, которые семантически похожи по ключевым словам, но абсолютно нерелевантны по сути запроса.

Чтобы решить эту проблему в промышленном RAG, применяется **двухэтапная архитектура извлечения данных**:

```ascii
========================================================================================
ДВУХЭТАПНАЯ АРХИТЕКТУРА ИЗВЛЕЧЕНИЯ: RETRIEVAL + RERANKING
========================================================================================

 Запрос пользователя
       |
       v
 +-----------+     Векторный поиск (Быстрый, дешевый)
 |  Stage 1  | ---------------------------------------> [ Извлекает Топ-50 кандидатов ]
 +-----------+                                                     |
                                                                   v
 +-----------+     Cohere Rerank API (Медленный, точный)           |
 |  Stage 2  | <---------------------------------------------------+
 +-----------+
       |
       v  (Cross-Encoder анализирует связку запрос-документ на глубоком уровне внимания)
 [ Топ-5 идеально точных документов ] -> Передаем в LLM
========================================================================================
```

### Этапы работы двухэтапного извлечения
1. **Этап 1: Retrieval (Извлечение).** Быстрый векторный поиск осуществляет грубую фильтрацию по всей базе знаний и извлекает относительно большую выборку кандидатов (например, Топ-50 документов). Этот этап дешев, масштабируем и занимает миллисекунды.
2. **Этап 2: Reranking (Переранжирование).** Мы передаем запрос пользователя и список 50 найденных документов в специализированную тяжелую кросс-энкодерную модель (например, через **Cohere Rerank API**).
Кросс-энкодеры не сжимают тексты в независимые векторы. Они принимают на вход пару *«Запрос + Документ» одновременно* и пропускают их через все слои механизма внимания (self-attention). Это позволяет модели оценить релевантность на глубоком семантическом уровне, учитывая логические связки, знаки препинания и контекст вопросов.
Модель Cohere Rerank пересчитывает оценки релевантности для каждого документа и возвращает отсортированный список, из которого мы берем Топ-5 для передачи в контекст генерации. Это снижает процент галлюцинаций более чем на 60% и гарантирует попадание только чистых фактов в промпт.

---

## Блок 4: Query Translation — генерация субзапросов и переписывание пользовательских запросов

Пользователи в реальной жизни редко пишут запросы в виде идеальных поисковых формулировок. Обычно их вопросы длинные, двусмысленные, содержат сленг или состоят из нескольких логически независимых подвопросов (например: *"Какова выручка нашей компании за 2025 год и какие основные риски были указаны в отчете за 3 квартал?"*). Если отправить такой запрос напрямую в векторную базу данных, результаты поиска будут хаотичными, так как вектор запроса попытается усреднить две совершенно разные темы.

Технологии **Query Translation** (Трансляция запросов) представляют собой слой предобработки перед поиском с использованием LLM:

1. **Query Rewriting (Переписывание запросов)**
   LLM переписывает исходный запрос пользователя, очищая его от грамматических ошибок, местоимений и сленга, преобразуя его в идеальный поисковый вектор. Например, запрос: *"Ну это, как там настроить отправку алертов в телегу из n8n при ошибке?"* превращается в: *"Настройка интеграции n8n с Telegram Bot API для отправки уведомлений о сбоях рабочих процессов"*.

2. **Sub-Query Generation (Генерация подзапросов)**
   Сложные запросы декомпозируются на массив простых независимых поисковых фраз. Запрос: *"Сравни выручку за 2024 и 2025 годы"* разбивается моделью на два подзапроса:
   - *"Выручка компании за 2024 год"*
   - *"Выручка компании за 2025 год"*
   Оба запроса выполняются в векторной базе параллельно, а результаты их извлечения объединяются. Это гарантирует, что система найдет документы по обеим темам.

3. **Multi-Query RAG (Мульти-запросный RAG)**
   LLM генерирует 3-5 синонимичных вариаций исходного запроса с разных точек зрения. Векторный поиск выполняется по всем вариациям. Это позволяет компенсировать ограниченность одной конкретной формулировки и извлечь документы, которые могли быть упущены из-за синонимических различий.

---

## Блок 5: Парсинг таблиц и схем — разбор сложных неструктурированных документов

Огромный массив корпоративных знаний хранится в PDF-отчетах, где ключевые данные представлены в виде таблиц, графиков и структурных диаграмм. Попытка прочитать такой файл стандартными библиотеками (вроде PyPDF или PDFMiner) превращает структурированную таблицу в сплошной, нечитаемый поток символов, где строки и столбцы перемешаны. Векторный поиск по такому контенту теряет всякий смысл, так как связи между ячейками полностью уничтожаются.

Для качественного извлечения табличных данных в продакшн-RAG применяются следующие подходы:

- **Использование специализированных парсеров (LlamaParse / Unstructured)**
  Эти сервисы используют глубокие нейросети для анализа структуры документа. Они распознают границы таблиц и преобразуют их в чистый Markdown-формат (с использованием синтаксиса `|---|---|`). Современные LLM идеально понимают Markdown-таблицы, считывая связи между заголовками столбцов и значениями ячеек.
  
- **Мультимодальный парсинг (Vision-based Retrieval)**
  Продвинутая стратегия, при которой страницы документа преобразуются в высокоразрешенные изображения. При извлечении система передает изображение страницы целиком в мультимодальную модель (например, Claude 3.5 Sonnet или GPT-4o) вместе с запросом. Зрительное внимание модели позволяет считывать информацию напрямую из графиков, чертежей и сложных перекрестных таблиц с точностью, недостижимой для текстовых парсеров.

---

## Блок 6: Векторное масштабирование — масштабирование индексов векторных баз данных под высокой нагрузкой

Когда объем вашей базы знаний вырастает с нескольких сотен документов до миллионов записей (например, база знаний крупного ритейлера или техническая документация авиастроительного холдинга), точный поиск ближайших векторов ( Flat / KNN поиск по косинусному сходству) начинает занимать секунды, что полностью парализует работу системы под нагрузкой.

Для обеспечения миллисекундного отклика применяются алгоритмы приближенного поиска (Approximate Nearest Neighbors - ANN):

1. **HNSW (Hierarchical Navigable Small World)**
   HNSW строит многослойную графовую структуру над векторным пространством, напоминающую концепцию Small World (социальные сети, где все связаны через несколько рукопожатий). На верхних слоях графа ребра длинные (для быстрой навигации по крупным кластерам), на нижних — короткие (для точечного поиска). Поиск начинается сверху и быстро спускается вниз, сокращая время поиска до O(log N). Это золотой стандарт для продакшна по скорости отклика, однако он требует значительных объемов оперативной памяти (RAM) для хранения графа.

2. **IVF (Inverted File Index)**
   IVF разделяет векторное пространство на тысячи кластеров с помощью алгоритма K-Means. При поиске система сначала находит несколько ближайших центроидов кластеров, а затем ищет только внутри этих выбранных кластеров, полностью игнорируя остальные 99% базы данных. Это существенно экономит вычислительные ресурсы.

3. **Квантование векторов (Product Quantization - PQ)**
   Технология сжатия векторов. Высокоразмерные векторы (например, 1536 float32 значений) сжимаются в короткие байтовые коды с некоторой потерей точности. Это позволяет уменьшить объем оперативной памяти, требуемой для хранения индексов в векторных базах (таких как pgvector, Pinecone, Qdrant или Milvus), в 4-8 раз, радикально снижая инфраструктурные расходы.

---

## Блок 7 (Python-разработка): Реализация логики Self-RAG (CRAG) на Python для автооценки релевантности документов

В данном практическом блоке мы спроектируем и реализуем на чистом Python архитектурный паттерн **Corrective RAG (CRAG)**. Этот паттерн вводит дополнительный слой контроля качества: он программно оценивает релевантность извлеченных из базы данных документов. Если оценка показывает, что документы не содержат ответа на вопрос (например, в базе знаний нет нужной информации), система динамически переключается на использование внешних поисковых инструментов (Tavily Search API) для поиска свежих данных в сети, гарантируя предоставление точного ответа в любых условиях.

Создадим файл и опишем логику работы CRAG-оценщика.

```python
import os
from typing import List, Dict, Any
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from pydantic import BaseModel, Field

class GradeDocument(BaseModel):
    \"\"\"Схема структурированного вывода для оценки релевантности документа.\"\"\"
    binary_score: str = Field(
        description="Релевантен ли документ запросу пользователя? Ответ строго 'yes' (да) или 'no' (нет)."
    )
    explanation: str = Field(
        description="Краткое обоснование принятого решения."
    )

class CorrectiveRAG:
    def __init__(self, openai_api_key: str):
        # Инициализируем модель с низкой температурой для детерминированности оценок
        self.llm = ChatOpenAI(
            model="gpt-4o-mini", 
            temperature=0, 
            openai_api_key=openai_api_key
        )
        # Настраиваем структурированный вывод на основе Pydantic-схемы
        self.grader = self.llm.with_structured_output(GradeDocument)
        
    def evaluate_retrieved_context(self, query: str, documents: List[str]) -> Dict[str, Any]:
        \"\"\"
        Проводит аудит качества извлеченного контекста.
        Возвращает список только релевантных документов и флаг необходимости веб-поиска.
        \"\"\"
        prompt = ChatPromptTemplate.from_messages([
            ("system", "Вы — строгий аудитор качества систем RAG. Ваша задача — оценить, содержит ли предоставленный "
                       "документ факты для ответа на запрос пользователя.\\n"
                       "Ответьте строго 'yes', если документ полезен, или 'no', если документ не содержит релевантной информации."),
            ("human", "Запрос пользователя: {query}\\n\\nДокумент из базы знаний: {document}")
        ])
        
        relevant_documents = []
        web_search_needed = False
        
        print(f"\\n--- Запуск CRAG Аудита для запроса: '{query}' ---")
        
        for i, doc in enumerate(documents):
            chain = prompt | self.grader
            try:
                result = chain.invoke({"query": query, "document": doc})
                print(f" Документ #{i+1}: Оценка = {result.binary_score.upper()} | Обоснование: {result.explanation}")
                
                if result.binary_score.lower() == "yes":
                    relevant_documents.append(doc)
                else:
                    # Если хотя бы один извлеченный документ нерелевантен, помечаем необходимость веб-поиска
                    web_search_needed = True
            except Exception as e:
                print(f" Ошибка при оценке документа #{i+1}: {e}")
                # В случае ошибки API сохраняем документ в качестве меры предосторожности
                relevant_documents.append(doc)
                
        return {
            "relevant_documents": relevant_documents,
            "web_search_needed": web_search_needed,
            "action_taken": "web_search_triggered" if web_search_needed and not relevant_documents else "vector_synthesis"
        }
```

Этот класс позволяет полностью автоматизировать отсев некачественного контекста. Если векторный поиск вернул "мусор", CRAG-оценщик перехватит его на этапе выполнения, предотвращая передачу некорректных данных в языковую модель.

---

## Блок 8: Архитектура Оценщик-Оптимизатор (Evaluator-Optimizer) для контроля качества генерации

Даже если извлеченный из векторной базы данных контекст идеален и полностью релевантен, остается риск того, что языковая модель на этапе генерации (synthesis) допустит логическую ошибку, упустит важные детали или проигнорирует часть предоставленных фактов.
Для обеспечения 100% надежности в продакшне мы внедряем паттерн **Evaluator-Optimizer** (Оценщик-Оптимизатор), который создает дополнительный замкнутый цикл самокоррекции на уровне приложения.

```ascii
========================================================================================
ПАТТЕРН САМОКОРРЕКЦИИ: EVALUATOR-OPTIMIZER
========================================================================================

    [ Извлеченный контекст ]
               |
               v
     +-------------------+
     | GENERATOR (LLM)   | <------------------------------------+
     | (Создает ответ)   |                                      |
     +-------------------+                                      |
               |                                                |
               v                                                | [ Ответ не прошел аудит ]
     +-------------------+                                      |   Передается фидбек
     | EVALUATOR (LLM)   | -------------------------------------+
     | (Проводит аудит)  |
     +-------------------+
               |
               v [ ОТВЕТ ВЕРИФИЦИРОВАН ] -> Отправка пользователю
========================================================================================
```

### Критерии оценки ответов
1. **Faithfulness (Верность контексту):**
   Оценщик проверяет, выводится ли каждое утверждение в ответе строго из предоставленных документов. Если модель утверждает что-то, чего нет в контексте, это классифицируется как галлюцинация, и ответ отклоняется.
2. **Answer Relevance (Соответствие запросу):**
   Оценщик проверяет, отвечает ли сгенерированный текст непосредственно на поставленный вопрос пользователя, не уходя в сторону и не перегружая ответ лишней информацией.
3. **Context Recall (Полнота извлечения):**
   Оценщик сопоставляет извлеченные документы и ответ, проверяя, были ли использованы все ключевые факты и метрики (цифры, имена, даты), предоставленные в качестве исходных данных.

Если ответ не проходит аудит, оценщик возвращает структурированный список замечаний генератору. Генератор запускает повторный цикл исправления (Self-Healing Loop), учитывая замечания оценщика.

---

## Блок 9: Проектирование сценариев 'заглушек' при отсутствии релевантных документов в векторной БД

При работе с клиентами в сфере B2B критически важно гарантировать предсказуемое поведение системы. Худшее, что может сделать ИИ-ассистент поддержки, если в базе знаний нет ответа на вопрос пользователя — начать придумывать ответ от себя или отвечать размытыми общими фразами.

Архитектура продвинутого RAG требует проектирования жестких детерминированных **сценариев отказа (Fallback Workflows)**:

- **Жесткие пороги уверенности (Similarity Thresholding):**
  Векторный поиск возвращает оценку сходства (например, cosine distance). Мы обязаны установить порог (например, не ниже 0.72 для моделей `text-embedding-3-small`). Если максимальная оценка среди всех извлеченных чанков ниже этого порога, система мгновенно прекращает попытки генерации через LLM.
  
- **Детерминированные заглушки:**
  При срабатывании порога система возвращает жестко зашитый, согласованный с юридическим отделом шаблон ответа: *"К сожалению, в официальных регламентах компании нет информации по вашему запросу. Я зарегистрировал обращение и передал его живому специалисту. Номер вашего тикета: #12932"*.
  
- **Эскалация в CRM/Helpdesk:**
  Вместо вывода ответа во фронтенд запускается фоновый подпроцесс n8n, который автоматически создает карточку инцидента в системе поддержки (например, Jira Service Desk или HubSpot CRM) с прикреплением всей истории диалога и сырых логов извлечения.

---

## Блок 10: Расчет ключевых RAG-метрик: Recall (полнота), Precision (точность) и Hit Rate

Управление качеством RAG-систем невозможно без регулярного численного измерения ключевых метрик на репрезентативных выборках (датасетах). Попытка оценивать качество "на глаз" приводит к регрессионным сбоям, когда улучшение ответа на один вопрос ломает логику извлечения на десяти других.

Мы внедряем методологию непрерывной оценки с использованием фреймворка **Ragas** и расчет следующих метрик:

1. **Hit Rate (Коэффициент попадания):**
   Показывает, как часто хотя бы один релевантный документ из эталонного датасета попадает в Топ-K документов, возвращенных векторным поиском. Целевой показатель в продакшне — не менее 92%.
   
2. **Context Precision (Точность контекста):**
   Оценивает качество ранжирования извлеченных документов. Релевантные чанки должны находиться строго на первых позициях (вверху выдачи), так как информация в начале контекста сильнее всего учитывается языковыми моделями при генерации (избегаем эффекта *Lost in the Middle*).
   
3. **Faithfulness Score (Коэффициент верности):**
   Математическая оценка отсутствия галлюцинаций. Вычисляется как отношение количества верифицированных утверждений в сгенерированном ответе к общему числу содержащихся в нем тезисов.
   
4. **Answer Semantic Similarity (Семантическое сходство ответа):**
   Сравнение сгенерированного ответа с эталонным экспертным ответом из тестового датасета с использованием векторного сравнения.

Внедрение регулярного прогона этих тестов в CI/CD пайплайны при каждом обновлении промптов или параметров чанкинга гарантирует долгосрочную стабильность вашей ИИ-инфраструктуры.
"""

    slides = """# Презентация: Продвинутый RAG для продакшна

---
📊 Слайд 1. Умный чанкинг
- Тема: Выход за рамки наивного разбиения (нарезка по 500 символов).
- Макет: Темный HSL фон, визуализация семантических разрывов.
- Ключевые моменты: Семантический чанкинг по косинусному сходству предложений, структурный чанкинг (Markdown), Layout-Aware.

---
📊 Слайд 2. Parent-Document Retriever
- Тема: Разделение задач семантического поиска и генерации ответов.
- Макет: Двухуровневая схема: мелкие Child-чанки и крупные Parent-документы.
- Ключевые моменты: Поиск по мелким векторам -> Передача крупного контекста в LLM -> Сохранение полной логики.

---
📊 Слайд 3. Переранжирование с Cohere Rerank
- Тема: Двухэтапная архитектура извлечения данных.
- Макет: Сравнение точности KNN-поиска и Cross-Encoder переранжирования.
- Ключевые моменты: Извлечение Топ-50 кандидатов -> Оценка через Cohere Rerank API -> Фильтрация до Топ-5.

---
📊 Слайд 4. Query Translation
- Тема: Предобработка сложных запросов пользователей.
- Макет: Схема распараллеливания запросов (Sub-queries).
- Ключевые моменты: Переписывание запросов (Query Rewriting), генерация подзапросов, Multi-Query.

---
📊 Слайд 5. Парсинг таблиц и схем
- Тема: Качественное извлечение структурированной информации из PDF.
- Макет: Изображение структуры таблицы в Markdown-формате.
- Ключевые моменты: LlamaParse, использование мультимодальных моделей (Claude 3.5 Sonnet / GPT-4o), OCR макеты.

---
📊 Слайд 6. Векторное масштабирование
- Тема: Приближенный поиск (ANN) на миллионах документов.
- Макет: Граф HNSW и квантование векторов.
- Ключевые моменты: HNSW, IVF индексы, продуктовое квантование (PQ) для оптимизации оперативной памяти.

---
📊 Слайд 7. Логика Corrective RAG (CRAG)
- Тема: Автоматическая коррекция RAG на Python.
- Макет: Блок-схема логики Self-RAG с ветвлением на веб-поиск.
- Ключевые моменты: Локальный оценщик релевантности документов, динамический перехват нерелевантных чанков, Tavily API.

---
📊 Слайд 8. Паттерн Evaluator-Optimizer
- Тема: Контроль галлюцинаций на уровне архитектуры.
- Макет: Интерактивная петля генерации и оценки.
- Ключевые моменты: Оценка Faithfulness, проверка Answer Relevance, Context Recall, автоматическая доработка ответа.

---
📊 Слайд 9. Проектирование заглушек и Fallback-сценариев
- Тема: Надежная обработка крайних случаев в RAG.
- Макет: Таблица принятия решений (Decision Matrix).
- Ключевые моменты: Детерминированные шаблоны отказа, жесткие пороги косинусного сходства, эскалация запросов в поддержку.

---
📊 Слайд 10. Метрики RAG-систем (Evals)
- Тема: Количественная оценка качества RAG в продакшне.
- Макет: Дашборд ключевых метрик с целевыми показателями.
- Ключевые моменты: Hit Rate, Faithfulness Score, Context Precision, интеграция систем непрерывной оценки Ragas.

"""

    video = """# Сценарий видео: Продвинутый RAG для продакшна

| Кадр / Scene | Звуковое сопровождение / Voiceover |
| :--- | :--- |
| **Кадр 1: Спикер крупным планом, на фоне схема RAG.** | Приветствую вас на 17-й неделе нашего курса! Сегодня мы разберем, как превратить обычный наколеночный RAG в надежную корпоративную систему, которая никогда не галлюцинирует и мгновенно находит самые точные ответы. |
| **Кадр 2: Анимация семантического разбиения предложений.** | Мы начнем с умного чанкинга. Забудьте про нарезку текста по 500 символов. Мы изучим семантический чанкинг на основе косинусного сходства предложений и Layout-Aware парсинг, сохраняющий целостность документов. |
| **Кадр 3: Визуализация Parent-Document Retriever.** | Далее мы разберем Parent-Document Retriever. Это мощная техника, которая позволяет искать информацию по мелким точечным дочерним чанкам, а передавать в LLM полные родительские документы, сохраняя широкий контекст. |
| **Кадр 4: Схема работы Cohere Rerank.** | Затем мы подключим переранжирование через Cohere Rerank API. Это позволит отфильтровать векторный мусор и вывести на первое место только на 100% релевантные факты с помощью кросс-энкодерных моделей. |
| **Кадр 5: Демонстрация Query Translation.** | Мы разберем Query Translation: как научить систему переписывать плохие запросы пользователей и дробить сложные задачи на несколько параллельных подзапросов для максимальной полноты выдачи. |
| **Кадр 6: Разбор сложной PDF таблицы на экране.** | Вы увидите, как правильно парсить неструктурированные PDF со сложными таблицами с помощью LlamaParse и мультимодальных моделей Claude 3.5 Sonnet, переводя их в чистый Markdown. |
| **Кадр 7: Код HNSW графа и графики производительности.** | Мы поговорим про масштабирование векторных баз данных под высокими нагрузками, используя алгоритмы приближенного поиска HNSW и квантование векторов для экономии RAM. |
| **Кадр 8: IDE с кодом класса CorrectiveRAG.** | В практической части по Python-разработке мы с нуля напишем логику Corrective RAG. Наш скрипт будет автоматически оценивать релевантность чанков и, если нужно, переключаться на веб-поиск. |
| **Кадр 9: Схема Evaluator-Optimizer.** | Мы спроектируем архитектуру Оценщик-Оптимизатор. Вы увидите, как дополнительный узел проверяет ответ модели на галлюцинации перед тем, как показать его конечному пользователю. |
| **Кадр 10: Дашборд с метриками Hit Rate и Ragas.** | Напоследок мы внедрим систему метрик и автоматических тестов с помощью Ragas, научимся измерять Hit Rate, Faithfulness и Context Precision. Поехали! |
"""

    return lecture, slides, video


def create_week_17_en():
    title = "Advanced Enterprise RAG for Production"
    
    lecture = """# Week 17. """ + title + """

Welcome to Week 17 of our professional curriculum for AI Automation Architects. This week, we will dive deep into one of the most critical and highly demanded topics in the modern artificial intelligence industry — the design, optimization, and deployment of production-grade Retrieval-Augmented Generation (RAG) systems.

While naive RAG works well for simple demonstration scripts and prototype chat applications, it inevitably collapses under the weight of real-world enterprise requirements: complex multi-page PDFs, dense financial tables, strict latency quotas, and a zero-tolerance policy for hallucinations.

Throughout this week, we will dismantle naive character-splitting strategies, implement a two-stage retrieval pipeline using the Parent-Document Retriever and Cohere Rerank, master query rewriting and sub-query deconstruction, parse highly complex document layouts, and scale vector databases. Finally, we will write a fully functional Corrective RAG (CRAG) system in Python and implement the Evaluator-Optimizer pattern to completely eradicate hallucinations.

---

## Block 1: Advanced Chunking Strategies — Beyond Naive Character Splitting

The naive approach to document preparation, which simply slices text into fixed blocks of characters (e.g., exactly 500 characters with a 50-character overlap), is the primary reason RAG systems fail in production environments. Logical arguments, definitions, and tables are rarely distributed uniformly. When a chunk boundary lands in the middle of a vital numerical value or tears a paragraph in half, the semantic vector of the resulting fragments becomes heavily distorted. Consequently, semantic searches return irrelevant context, leading to model hallucinations during the synthesis phase.

### Advanced Chunking Techniques

1. **Semantic Chunking**
   Rather than relying on arbitrary character counts, semantic chunking leverages the embedding model itself to locate natural, logical boundaries. The process is as follows:
   - The document text is split into individual sentences using regex or libraries like NLTK/Spacy.
   - A vector embedding is generated for each individual sentence.
   - Cosine similarity is computed between consecutive sentences.
   - By analyzing the semantic distances, the system identifies sharp drops in similarity (e.g., below a statistical threshold like the 95th percentile). These drops represent the natural transitions between distinct ideas and are set as chunk boundaries.
   This guarantees that every chunk represents a semantically complete and logical unit of information.

2. **Structural Chunking**
   This method relies on document structure (Markdown headings, HTML tags, XML schema structures). Splitting is executed strictly along document boundaries (`#`, `##`, `###`), preserving the hierarchical relationship of the text. For example, when chunking a user manual, each chunk will strictly correspond to an isolated instruction block, preventing overlap with neighboring sections.

3. **Layout-Aware Chunking**
   Designed specifically for PDFs and scanned documents. Using libraries like PDF-Plumber, LayoutParser, or cloud-based OCR engines, the document is segmented into visual bounding boxes (main text, columns, tables, sidebars). Chunks are built along these physical boundaries, ensuring that headers, footers, and page numbers do not pollute the semantic vector.

```ascii
========================================================================================
SEMANTIC CHUNKING: IDENTIFYING BOUNDARIES BASED ON EMBEDDING DISTANCES
========================================================================================

 Sentence 1 -----> [Embedding 1] 
                                  \\  Cosine Similarity: 0.94 (High -> Group together)
 Sentence 2 -----> [Embedding 2] /
                                  \\  Cosine Similarity: 0.72 (Low -> CHUNK BOUNDARY!)
-------------------------- CHUNK BOUNDARY ----------------------------------------------
 Sentence 3 -----> [Embedding 3] /
========================================================================================
```

### Optimal Overlap Design
Overlap is critical to maintaining context continuity at chunk boundaries. In advanced RAG systems, overlap sizes are dynamic. For semantic chunking, an overlap of 1-2 sentences is recommended, ensuring that the transition from one concept to the next is smooth and that vital pronouns or reference hooks are not lost.

---

## Block 2: Parent-Document Retriever — Separating Retrieval from Synthesis

During RAG design, architects face a fundamental contradiction between information retrieval and response synthesis:
- **For optimal search accuracy**, the vector database needs small, clean, and highly focused text chunks (100-250 tokens). Small chunks lack irrelevant "noise," which yields distinct semantic vectors and significantly improves matching accuracy.
- **For high-quality response synthesis**, the LLM requires a broad, detailed context window (1000-2000 tokens). The model needs the surrounding details, background reasoning, and conditions, or it will generate a generic, incomplete answer.

The **Parent-Document Retriever** pattern resolves this contradiction by completely decoupling the search index from the synthesis context.

```ascii
========================================================================================
ARCHITECTURE DIAGRAM: PARENT-DOCUMENT RETRIEVER
========================================================================================

     [ Primary Document Storage ] <--------------------------------------+
                 |                                                       |
                 | 1. Stores complete parent document                    |
                 v                                                       |
     [ Parent Doc: 2000 tokens ]                                         |
                 |                                                       |
                 | 2. Slices into child chunks                           |
                 v                                                       |
        +--------+--------+                                              |
        |                 |                                              |
        v                 v                                              |
   [Child 1: 150t]   [Child 2: 150t] -----> [ Vector Database Index ]    |
                                                     |                   |
                                                     | 3. Search matches |
                                                     |    Child 2        |
                                                     v                   |
                                            [ Retrieve Parent ] ---------+
                                            (Feeds 2000 tokens to LLM)
========================================================================================
```

### Decoupled Retrieval Execution Flow
1. Source documents are stored in a primary relational or document database (e.g., PostgreSQL or MongoDB) as complete, rich text blocks — these are the **Parent Documents**. Each parent is assigned a unique `parent_id`.
2. Each parent document is sliced into numerous small, focused child fragments — **Child Chunks** (typically 100-150 tokens each).
3. Embeddings are generated for each child chunk and saved in a vector database, with the metadata mapping back to the corresponding `parent_id`.
4. At query time, the system performs a semantic similarity search strictly across the child chunks in the vector database.
5. Once the most relevant child chunk is matched, the system retrieves its `parent_id` from the metadata, queries the primary document store, pulls the **entire parent document**, and feeds it to the LLM.
This strategy enables surgical search precision while supplying the LLM with a comprehensive context window.

---

## Block 3: Reranking — Integrating Cohere Rerank API to Filter Vector Search Outputs

Traditional semantic similarity search (using vector dot product or cosine distance) is optimized for high-speed indexing and math operations across massive high-dimensional spaces. However, it suffers from a major bottleneck: embedding models compress an entire paragraph's meaning into a single fixed-size vector (e.g., 1536 values), losing fine-grained logical negation, specific numbers, and syntax details. Consequently, vector searches often return documents that match keywords semantically but miss the logic of the query.

To solve this in enterprise-grade RAG, we implement a **two-stage retrieval pipeline**:

```ascii
========================================================================================
TWO-STAGE RETRIEVAL PIPELINE: RETRIEVAL + RERANKING
========================================================================================

 User Query
     |
     v
 +-----------+     Vector Search (Fast, cheap index sweep)
 |  Stage 1  | ---------------------------------------> [ Retrieves Top-50 Candidates ]
 +-----------+                                                     |
                                                                   v
 +-----------+     Cohere Rerank API (Slower, precise)             |
 |  Stage 2  | <---------------------------------------------------+
 +-----------+
       |
       v  (Cross-Encoder computes query-document relevance at attention layer)
 [ Top-5 Highly Grounded Documents ] -> Fed to LLM
========================================================================================
```

### Retrieval and Re-ranking Execution
1. **Stage 1: Retrieval.** A rapid vector search sweeps the entire database, pulling a rough list of candidates (e.g., Top-50 documents). This stage is fast, highly scalable, and takes milliseconds.
2. **Stage 2: Reranking.** The user query and the Top-50 retrieved documents are sent to a heavy cross-encoder model (such as the **Cohere Rerank API**).
Unlike embeddings, cross-encoders do not evaluate documents independently. They accept the *query and the document simultaneously* as a single input, passing them through all layers of the self-attention mechanism. This enables the model to assess factual matching, precise phrasing, and contextual logic.
The Cohere Rerank model re-computes similarity scores and returns a re-ordered list. We pass only the Top-5 documents to the LLM prompt. This reduces hallucinations by over 60% and ensures that only highly relevant facts are processed.

---

## Block 4: Query Translation — Query Deconstruction and Rewriting

In real-world settings, users rarely write perfectly structured queries. Instead, questions are often long, highly conversational, and contain multiple distinct sub-questions (e.g., *"What was our Q3 revenue, and what were the primary risks outlined in our annual report?"*). If we feed this raw query directly into a vector database, the search quality will be poor because the vector representing the query averages out two completely different topics.

**Query Translation** acts as a preprocessing layer using LLMs before search execution:

1. **Query Rewriting**
   An LLM rewrites the user's raw input to clean up typos, conversational filler, and slang, transforming it into an optimal search vector. For example, *"Hey, so how do I set up Slack notifications on n8n errors?"* becomes *"How to configure Slack notifications for failed workflows in n8n"*.

2. **Sub-Query Generation**
   Complex queries are decomposed into a list of simpler, independent search queries. The query *"Compare our sales metrics for 2024 and 2025"* is broken down into:
   - *"Company sales metrics for 2024"*
   - *"Company sales metrics for 2025"*
   Both sub-queries run in parallel in the vector database, and the retrieved chunks are combined. This guarantees that both years are covered in the search results.

3. **Multi-Query RAG**
   The LLM generates 3-5 synonymous variations of the input query from different semantic perspectives. A vector search runs for all variations. This overcomes the limitations of single-word phrasing and ensures that no relevant document is missed due to synonym mismatches.

---

## Block 5: Parsing Tables and Schemas — Extracting Data from Complex Layouts

Much of an enterprise's data is trapped in PDF files where critical information is formatted as tables, schematics, and flowcharts. Parsing such files with basic text extractors (like PyPDF or PDFMiner) flattens structured columns into a chaotic, unreadable stream of words, destroying the logical relationships between rows and headers. Vector searches over such flattened data fail because the context is lost.

To extract tabular data reliably, we use:

- **Specialized Structural Parsers (LlamaParse / Unstructured)**
  These tools use deep learning models to identify table boundaries and convert them into clean Markdown format (using `|---|---|`). Modern LLMs are highly proficient at reading Markdown tables, preserving the relationships between headers and row values.
  
- **Multimodal Vision-Based Retrieval**
  An advanced strategy where PDF pages are rendered directly into high-resolution images. During retrieval, the page image is passed to a vision-language model (VLM like Claude 3.5 Sonnet or GPT-4o) alongside the query. The model's visual attention parses charts, diagrams, and complex tables directly, bypassing text extraction issues entirely.

---

## Block 6: Vector Scalability — Scaling Vector Indexes under High Concurrent Loads

As your document store expands from a few hundred documents to millions of rows, performing exact k-nearest neighbor (KNN/Flat) vector searches becomes computationally expensive, leading to high latency under load.

To maintain millisecond latency, we implement Approximate Nearest Neighbors (ANN) indexing:

1. **HNSW (Hierarchical Navigable Small World)**
   HNSW constructs a multi-layer graph over the vector space, utilizing the "small world" network concept. The top layers contain long-range connections for fast cluster hopping, while the lower layers contain short-range connections for fine-grained localized searches. Search begins at the top and navigates down, reducing search complexity to O(log N). HNSW is highly popular in production due to its speed, though it requires substantial RAM to hold the graph.

2. **IVF (Inverted File Index)**
   IVF partition the vector space into thousands of distinct clusters using K-Means. At search time, the query vector is compared against cluster centroids, and search is restricted only to the closest clusters, skipping the rest of the database and saving CPU cycles.

3. **Product Quantization (PQ)**
   A vector compression technique. High-dimensional float32 vectors are compressed into shorter byte codes. This reduces the memory footprint of vector indexes in databases like pgvector, Qdrant, or Pinecone by up to 4-8x, drastically cutting infrastructure costs.

---

## Block 7 (Python Development): Implementing Corrective RAG (CRAG) in Python

In this practical development lab, we will design and implement the **Corrective RAG (CRAG)** architectural pattern in Python. This pattern introduces a validation layer: it programmatically evaluates the relevance of retrieved documents. If the evaluation indicates that the documents lack necessary facts, the system dynamically falls back to an external web search tool (Tavily Search API), ensuring a grounded and accurate answer under all conditions.

Let's write the complete code.

```python
import os
from typing import List, Dict, Any
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from pydantic import BaseModel, Field

class GradeDocument(BaseModel):
    \"\"\"Structured schema for document relevance grading.\"\"\"
    binary_score: str = Field(
        description="Is the document relevant to the user query? Respond strictly with 'yes' or 'no'."
    )
    explanation: str = Field(
        description="A brief explanation justifying the decision."
    )

class CorrectiveRAG:
    def __init__(self, openai_api_key: str):
        # Initialize model with zero temperature for deterministic grading
        self.llm = ChatOpenAI(
            model="gpt-4o-mini", 
            temperature=0, 
            openai_api_key=openai_api_key
        )
        self.grader = self.llm.with_structured_output(GradeDocument)
        
    def evaluate_retrieved_context(self, query: str, documents: List[str]) -> Dict[str, Any]:
        \"\"\"
        Grades retrieved document context.
        Returns a list of relevant documents and a flag indicating if a web search is needed.
        \"\"\"
        prompt = ChatPromptTemplate.from_messages([
            ("system", "You are a strict RAG auditor. Grade if the provided document contains "
                       "facts that are useful to answer the user query.\\n"
                       "Respond with 'yes' if the document is relevant, or 'no' if it is irrelevant."),
            ("human", "Query: {query}\\n\\nDocument: {document}")
        ])
        
        relevant_documents = []
        web_search_needed = False
        
        print(f"\\n--- Starting CRAG Audit for query: '{query}' ---")
        
        for i, doc in enumerate(documents):
            chain = prompt | self.grader
            try:
                result = chain.invoke({"query": query, "document": doc})
                print(f" Doc #{i+1}: Grade = {result.binary_score.upper()} | Reason: {result.explanation}")
                
                if result.binary_score.lower() == "yes":
                    relevant_documents.append(doc)
                else:
                    web_search_needed = True
            except Exception as e:
                print(f" Error grading doc #{i+1}: {e}")
                relevant_documents.append(doc)
                
        return {
            "relevant_documents": relevant_documents,
            "web_search_needed": web_search_needed,
            "action_taken": "web_search_triggered" if web_search_needed and not relevant_documents else "vector_synthesis"
        }
```

This class allows you to filter out poor context. If the vector search returns irrelevant documents, the CRAG validator catches them, preventing the LLM from synthesizing an incorrect answer.

---

## Block 8: Evaluator-Optimizer Architecture for Quality Control

Even when the retrieved context is highly relevant, there is a risk that the LLM might make a logical error, miss a detail, or ignore part of the provided facts during synthesis.
To ensure quality, we implement the **Evaluator-Optimizer** pattern, which establishes a self-correcting feedback loop at the application layer.

```ascii
========================================================================================
SELF-CORRECTION LOOP: EVALUATOR-OPTIMIZER
========================================================================================

    [ Retrieved Context ]
               |
               v
     +-------------------+
     | GENERATOR (LLM)   | <------------------------------------+
     | (Generates Draft) |                                      |
     +-------------------+                                      |
               |                                                |
               v                                                | [ Feedback Loop ]
     +-------------------+                                      |   Audit failed,
     | EVALUATOR (LLM)   | -------------------------------------+   send correction notes
     | (Audits Response) |
     +-------------------+
               |
               v [ VERIFIED RESPONSE ] -> Sent to User
========================================================================================
```

### Response Evaluation Criteria
1. **Faithfulness:**
   The evaluator verifies that every claim in the response is supported by the retrieved documents. If the model claims something not present in the context, it is flagged as a hallucination, and the draft is rejected.
2. **Answer Relevance:**
   The evaluator checks if the response directly addresses the user's question without adding irrelevant filler.
3. **Context Recall:**
   The evaluator matches the retrieved documents with the response to ensure all key metrics (numbers, names, dates) have been utilized.

If the draft fails the audit, the evaluator returns a list of correction notes to the generator, which triggers a self-healing loop to refine the answer.

---

## Block 9: Designing Fallbacks for Out-of-Distribution Queries

For enterprise B2B applications, predictable behavior is critical. The worst thing an automated support agent can do when it lacks information in its database is make up an answer or reply with vague, unhelpful sentences.

Advanced RAG architectures enforce strict **Fallback Workflows**:

- **Similarity Thresholds:**
  Vector searches return similarity scores. We must set a strict threshold (e.g., minimum 0.72 for `text-embedding-3-small` models). If no chunk scores above this threshold, the system halts LLM generation.
  
- **Deterministic Refusal Templates:**
  When the threshold check fails, the system returns a pre-approved template: *"I am sorry, but I couldn't find information in our official database. I have registered your request and forwarded it to a support specialist. Ticket ID: #12932"*.
  
- **Automated Helpdesk Escalation:**
  An n8n sub-workflow runs in the background to automatically create an incident card in the support system (e.g., Jira Service Desk or HubSpot CRM) and attaches the conversation history and retrieval logs.

---

## Block 10: RAG Metrics and Evaluations — Hit Rate, Precision, and Recall

Evaluating RAG quality cannot rely on manual, subjective reviews. Doing so often leads to regression issues, where fixing one query breaks ten others.

We implement automated evaluation suites using the **Ragas** framework to measure performance over curated evaluation datasets:

1. **Hit Rate:**
   Measures how often a relevant document from the ground-truth dataset is included in the top-K retrieved results. The production target is >= 92%.
   
2. **Context Precision:**
   Evaluates whether relevant chunks are ranked higher in the results (ranked at the top), avoiding *Lost in the Middle* performance drops.
   
3. **Faithfulness Score:**
   A metric representing the absence of hallucinations. It is computed as the ratio of verifiable claims in the generated response to the total number of claims made.
   
4. **Answer Semantic Similarity:**
   Comparing the generated response against a ground-truth expert answer using semantic embeddings.

Running these evaluations in your CI/CD pipelines ensures long-term stability and reliability for your AI infrastructure.
"""

    slides = """# Slides: Advanced Enterprise RAG for Production

---
📊 Slide 1. Advanced Chunking Strategies
- Title: Going beyond naive character splitting.
- Layout: Dark HSL background, visual representation of semantic sentence gaps.
- Key Points: Semantic chunking via sentence similarity, structural Markdown splits, Layout-Aware chunking.

---
📊 Slide 2. Parent-Document Retriever
- Title: Separating search indices from synthesis contexts.
- Layout: Two-level hierarchy diagram showing Child chunks and Parent documents.
- Key Points: Fast vector search on child chunks -> Mapping back to parent documents -> Providing rich context for synthesis.

---
📊 Slide 3. Re-ranking with Cohere Rerank
- Title: Two-stage retrieval pipelines.
- Layout: Comparison between KNN vector search and Cross-Encoder re-ranking.
- Key Points: Retrieve Top-50 candidates -> Re-rank via Cohere API -> Keep only the Top-5 most relevant.

---
📊 Slide 4. Query Translation
- Title: Preprocessing complex user queries.
- Layout: Parallel execution flowchart showing sub-queries.
- Key Points: Query Rewriting, Sub-query generation, Multi-Query RAG to bypass embedding limits.

---
📊 Slide 5. Parsing Tables and Layouts
- Title: Structured information extraction from PDFs.
- Layout: Table representation converted to Markdown format.
- Key Points: LlamaParse, Multimodal extraction with Claude 3.5 Sonnet, Layout-aware OCR grids.

---
📊 Slide 6. Scaling Vector Indexes
- Title: High-throughput Approximate Nearest Neighbors (ANN).
- Layout: HNSW graph visualization and vector product quantization.
- Key Points: HNSW graphs, IVF indexes, Product Quantization (PQ) to optimize RAM footprint.

---
📊 Slide 7. Corrective RAG (CRAG)
- Title: Automated RAG quality correction in Python.
- Layout: Code flow chart leading to fallback search routing.
- Key Points: Python-based document grading class, dynamic search triggers, Tavily API integration.

---
📊 Slide 8. Evaluator-Optimizer Pattern
- Title: Eliminating hallucinations via dual-agent loops.
- Layout: Circular generation and evaluation feedback loop.
- Key Points: Faithfulness audits, Answer relevance grading, Context recall verification, automated corrections.

---
📊 Slide 9. Designing Fallback Workflows
- Title: Enforcing robust exception handling in production.
- Layout: If/Else decision matrix for low-similarity matches.
- Key Points: Deterministic refusal templates, similarity thresholds, automated ticketing escalations.

---
📊 Slide 10. Quantitative RAG Evaluations
- Title: Measuring performance scientifically in CI/CD.
- Layout: Metrics dashboard showing target KPIs.
- Key Points: Hit Rate, Faithfulness Index, Context Precision, continuous integration testing with Ragas.

"""

    video = """# Video Script: Advanced Enterprise RAG for Production

| Scene | Voiceover |
| :--- | :--- |
| **Scene 1: Close-up of speaker, RAG diagram on background.** | Welcome to Week 17 of our course! Today, we will discuss how to turn basic RAG pipelines into robust, enterprise-grade systems that never hallucinate and always locate the exact facts. |
| **Scene 2: Animation showing semantic sentence boundaries.** | We will start with advanced chunking. Forget about character splitting. We'll explore semantic sentence splitting and layout-aware parsing that preserves tables and document hierarchies. |
| **Scene 3: Parent-Document Retriever diagram.** | Next, we'll dive into the Parent-Document Retriever. This technique enables searching through small, highly focused child chunks while supplying large parent documents to the LLM for rich context. |
| **Scene 4: Cohere Rerank workflow on screen.** | We'll then implement a two-stage retrieval pipeline using Cohere Rerank to filter out vector noise and keep only the top 5 most relevant documents. |
| **Scene 5: Diagram of Query Translation.** | We'll explore Query Translation, learning how to rewrite vague queries and break complex requests down into multiple parallel sub-queries. |
| **Scene 6: Complex PDF table being parsed into Markdown.** | You'll see how to parse complex PDF layouts and tables into clean Markdown using LlamaParse and Claude 3.5 Sonnet. |
| **Scene 7: High-scale HNSW graphs on screen.** | We'll discuss scaling vector databases under high load using Approximate Nearest Neighbors like HNSW and vector quantization. |
| **Scene 8: IDE showing CorrectiveRAG Python class.** | In our Python development lab, we'll build a Corrective RAG pipeline from scratch that grades document relevance and routes to web search fallbacks. |
| **Scene 9: Evaluator-Optimizer loop.** | We'll design an Evaluator-Optimizer architecture to audit generated answers for faithfulness before they reach the user. |
| **Scene 10: Ragas evaluation dashboard.** | Finally, we'll set up automated evaluation suites using Ragas to measure Hit Rate, Faithfulness, and Context Precision. Let's get started! |
"""

    return lecture, slides, video


def create_week_26_ru():
    title = "Капстон-проект и Карьерный старт (Дипломный проект)"
    
    lecture = """# Неделя 26. """ + title + """

Добро пожаловать на 26-ю неделю нашего профессионального курса для AI Automation Architects! Позади 6 месяцев интенсивной работы: вы прошли путь от понимания архитектуры REST API и создания визуальных воркфлоу в n8n до асинхронной разработки на Python, построения сложных мультиагентных графов состояний на базе LangGraph и внедрения промышленных методологий контроля надежности ИИ (Harness Engineering).

Выпускная неделя полностью посвящена подготовке и защите вашего выпускного капстон-проекта (Capstone Project), а также правильному выходу на рынок в качестве высококлассного и высокооплачиваемого специалиста. Мы разберем стратегии карьерного старта на фриланс-биржах, научимся проводить глубокий предпроектный аудит бизнес-процессов клиентов, составлять юридически безопасные договоры, упаковывать Loom-портфолио и масштабировать вашу практику от соло-разработчика до собственного ИИ-агентства с маржинальностью бизнеса до 80%.

---

## Блок 1: Упаковка услуг — позиционирование и упаковка услуг автоматизации бизнеса • Ценообразование на основе ценности (Value-based pricing)

Большинство инженеров при выходе на фриланс или при общении с B2B-клиентами совершают критическую ошибку: они пытаются продавать технологии. Они пишут в коммерческих предложениях: *"Я настраиваю n8n, пишу скрипты на Python и работаю с векторной базой Qdrant"*. Для собственника бизнеса эти слова звучат как абракадабра. Бизнес не покупает технологии. Бизнес покупает решения своих конкретных проблем: сокращение операционных расходов, увеличение скорости обработки заявок, экономию фонда оплаты труда (ФОТ) или рост объема продаж.

### Сдвиг в позиционировании
Вы должны позиционировать себя не как "технический исполнитель", а как **AI Automation Architect** — стратегический партнер, который трансформирует операционные процессы компании с помощью ИИ. 
- *Неправильно:* *"Я соберу вам чат-бота поддержки на n8n за $500"*.
- *Правильно:* *"Я внедрю автоматическую систему обработки обращений на базе ИИ с точностью ответов 98%. Это позволит вам сократить штат поддержки с 10 до 2 сотрудников, экономя $6,000 ежемесячно при росте скорости ответа до 2 секунд"*.

### Методология ценностного ценообразования (Value-Based Pricing)
Если вы продаете свое время по часам, ваш доход жестко ограничен суточным лимитом времени. Более того, чем быстрее и профессиональнее вы решаете задачу, тем меньше вам платят.
Вы обязаны перейти на модель **Value-Based Pricing**:
1. Проведите аудит и рассчитайте экономическую выгоду (ROI) вашего решения за год.
2. Если внедрение вашей системы экономит клиенту $72,000 в год, ценность вашего решения огромна.
3. Вы можете смело оценивать свои услуги в 20-30% от годовой экономии — например, установить стоимость проекта в $18,000. Для клиента это выгодная сделка, так как проект окупается всего за 3 месяца, а далее приносит чистую прибыль.

```ascii
========================================================================================
VALUE-BASED PRICING MODEL vs HOURLY BILLING
========================================================================================

 [ ПОЧАСОВАЯ ОПЛАТА ]:  50 часов работы * $50/час = $2,500 (Клиент торгуется за каждый час)
 
 [ VALUE-BASED ]:       Ценность для бизнеса: экономия $72,000/год.
                        Стоимость проекта: $18,000 (Клиент платит за ROI и окупаемость)
========================================================================================
```

---

## Блок 2: Фриланс-старт — составление профессиональных откликов и предложений на Upwork и Habr Freelance

Выход на рынок фриланса (Upwork, Habr Freelance, Kwork) требует строгого соблюдения правил составления откликов (proposals). Шаблонные, сгенерированные в ChatGPT отклики вроде *"Привет, я опытный разработчик ИИ, готов сделать ваш проект"* моментально удаляются клиентами.

### Фреймворк идеального отклика (The Winning Proposal)

- **Первые две строчки (The Hook):**
  Это единственное, что видит клиент в превью откликов, не открывая их целиком. Вы обязаны захватить его внимание. Вместо представления себя сразу покажите глубокое понимание его проблемы: *"Я внимательно изучил вашу задачу по синхронизации лидов из Typeform в HubSpot через n8n. У вас наверняка возникнет проблема с дублированием контактов, если пользователи отправляют форму несколько раз — вот как я решаю это на уровне архитектуры..."*
  
- **Конкретное решение проблемы:**
  Кратко опишите верхнеуровневую архитектуру решения (3-4 предложения). Покажите, что вы уже знаете, как реализовать проект.
  
- **Социальное подтверждение (Social Proof):**
  Приложите ссылки на 1-2 ваших прошлых похожих кейса с конкретными цифрами результатов.
  
- **Zoom Call Call-to-Action (CTA):**
  Закончите отклик предложением созвониться: *"Давайте созвонимся на 10 минут в Zoom завтра. Я покажу вам демо-стенд готовой интеграции HubSpot, которую собирал для аналогичного проекта. В какое время вам удобно?"*

---

## Блок 3: Loom-портфолио — запись эффективных видеодемонстраций созданных ИИ-решений

Клиенты в B2B-сегменте не умеют читать код в Github и не верят красивым текстовым описаниям в портфолио. Лучший инструмент продаж для AI-архитектора — это **Loom-видеодемонстрация** ваших готовых систем. Это короткие (2-3 минуты) записи экрана, где вы показываете работу системы в реальном времени.

### Фреймворк записи Loom-видео
1. **The Hook (Проблема) — первые 15 секунд:**
   *"В этом видео я покажу, как мы автоматизировали отдел продаж для компании по аренде спецтехники, сократив время квалификации лида с 2 часов до 45 секунд"*.
2. **Как это устроено (Under the Hood) — 60 секунд:**
   Откройте холст n8n или граф LangGraph. Проведите клиента по основным узлам простыми словами: *"Вот здесь вебхук принимает заявку, этот ИИ-узел анализирует текст сообщения, а этот модуль обращается к CRM"*. Это доказывает вашу глубокую инженерную экспертизу.
3. **The Magic (Результат) — 45 секунд:**
   Покажите "магию" в реальном времени. Отправьте тестовую заявку и покажите, как через 10 секунд в CRM создается карточка сделки, полностью заполненная обогащенными данными с готовым ИИ-черновиком ответа.

Наличие 5-6 таких Loom-видео в вашем портфолио заменяет десятки текстовых страниц и закрывает 80% возражений клиентов еще до созвона.

---

## Блок 4: Аудит процессов — разработка чек-листов аудита бизнес-процессов клиентов для выявления зон автоматизации

Приступать к сборке автоматизации без проведения глубокого предпроектного аудита — это гарантированный способ провалить проект. Клиенты часто сами не понимают, как устроены их бизнес-процессы, или описывают их хаотично. Ваша задача — навести порядок.

Вы обязаны проводить **платный Discovery-аудит** по следующему чек-листу:

- [ ] **Картирование процесса "As-Is" (Как есть):**
  Пошаговое описание текущего ручного выполнения задачи. Кто, куда, какую строчку копирует? Какие файлы перетаскиваются вручную?
- [ ] **Выявление узких мест (Bottlenecks):**
  Где скапливается больше всего рутины? На каком этапе клиенты чаще всего уходят из-за долгого ответа менеджера?
- [ ] **Инвентаризация данных:**
  Какие API доступны у CRM, ERP, мессенджеров и баз данных клиента? Есть ли у них документация?
- [ ] **Экономический расчет:**
  Сколько обращений/лидов проходит через компанию в месяц? Сколько времени тратится на один лид? Это необходимо для расчета окупаемости проекта и будущих расходов на API-токены.

По итогам аудита вы предоставляете клиенту документ архитектуры "To-Be" (Как будет), фиксируя точные рамки проекта (Scope of Work).

---

## Блок 5: Договоры автоматизации — юридические аспекты и разграничение ответственности

Внедрение искусственного интеллекта в бизнес связано с уникальными юридическими рисками. Языковые модели могут галлюцинировать (выдавать ложную информацию за истину), API провайдеров могут падать, а при работе с персональными данными клиентов необходимо соблюдать строгие законы конфиденциальности. Ваши юридические договоры должны надежно защищать вас от этих рисков.

### Ключевые пункты B2B договора автоматизации

1. **Дисклеймер о вероятностном характере ИИ:**
   В договоре должно быть четко зафиксировано, что результаты работы нейросетей являются продуктом вероятностного моделирования. Исполнитель не несет ответственности за единичные неточности, галлюцинации или ошибки генерации ИИ, если система в целом соответствует утвержденным приемочным тестам.
2. **Разграничение ответственности за сторонние API (SLA):**
   Вы не несете ответственности за технические сбои, замедления или изменения тарифной политики на серверах OpenAI, Anthropic, Qdrant, n8n Cloud или HubSpot. Любые сбои внешних инфраструктур выводятся из вашей зоны ответственности.
3. **Безопасность данных (Data Privacy):**
   В договоре должно быть прописано, что клиент обязуется не передавать в систему персональные данные пользователей (например, номера паспортов), а вы обязуетесь использовать API-режимы OpenAI/Anthropic, исключающие обучение моделей на данных клиента.

---

## Блок 6: Запуск агентства — масштабирование от соло-разработчика к собственной студии

Заработав первые стабильные $5,000–$10,000 в месяц на фрилансе и прямых продажах, вы неизбежно упретесь в потолок личной производительности. Вы больше не сможете брать новые проекты, так как все ваше время будет уходить на кодинг и сборку воркфлоу.
Масштабирование бизнеса требует перехода к **агентской модели**:

- **Делегирование рутины (Junior Engineers):**
  Наймите младших разработчиков. Передайте им рутинные задачи: сборку базовых линейных воркфлоу в n8n, написание простых скриптов-парсеров на Python и верстку интерфейсов. Ваше время должно быть сфокусировано на архитектуре, продажах и общении с ключевыми клиентами.
- **Создание IP Assets (Интеллектуальной собственности):**
  Вместо разработки каждого проекта абсолютно с нуля, создавайте универсальные, повторно используемые модули (шаблоны RAG, универсальные коннекторы CRM-Telegram, системы трейсинга). Это позволяет собирать новые проекты для клиентов за 2-3 дня вместо месяца, повышая маржинальность вашего бизнеса до 80-90%.

---

## Блок 7 (Python-разработка): Разработка комплексной программной архитектуры выпускного проекта на чистом Python с использованием асинхронных библиотек

В рамках выпускного проекта вы должны продемонстрировать глубокое владение профессиональным асинхронным стеком на Python. Мы разработаем ядро веб-сервиса на базе **FastAPI**, координирующее прием входящих заявок (вебхуков) от CRM, и запускающее фоновую асинхронную цепочку ИИ-агентов с использованием библиотеки `httpx` для параллельных запросов.

```python
import asyncio
from fastapi import FastAPI, BackgroundTasks, HTTPException
from pydantic import BaseModel, EmailStr
import httpx
import logging

# Настройка логирования для отслеживания шагов в продакшне
logging.basicConfig(level=logging.INFO, format="%(asctime)s - %(levelname)s - %(message)s")
logger = logging.getLogger("CapstoneOrchestrator")

app = FastAPI(
    title="Capstone AI Agent Orchestrator",
    description="Промышленное асинхронное ядро для координации ИИ-агентов компании",
    version="1.0.0"
)

class LeadWebhook(BaseModel):
    lead_id: str
    email: str
    raw_text: str

async def process_lead_pipeline_async(lead_id: str, email: str, raw_text: str):
    \"\"\"
    Асинхронный конвейер обработки лида ИИ-агентами.
    Включает семантическое обогащение, ICP-валидацию и отправку в CRM.
    \"\"\"
    logger.info(f"[PIPELINE START] Начало обработки лида {lead_id}")
    
    # Симулируем асинхронное обращение к LLM для суммаризации боли клиента (2 секунды)
    await asyncio.sleep(2)
    logger.info(f"[AGENT 1] Запрос суммаризован для {lead_id}")
    
    # Симулируем асинхронный поиск по регламентам RAG (3 секунды)
    await asyncio.sleep(3)
    logger.info(f"[AGENT 2] Ответ по базе знаний подготовлен для {lead_id}")
    
    # В реальном проекте здесь будет отправка через асинхронный клиент httpx:
    # async with httpx.AsyncClient() as client:
    #     await client.post("https://crm.client.com/api/leads", json=...)
    
    logger.info(f"[PIPELINE SUCCESS] Лид {lead_id} успешно сохранен в CRM. Задача завершена.")

@app.post("/api/v1/leads", status_code=202)
async def receive_lead_webhook(payload: LeadWebhook, background_tasks: BackgroundTasks):
    \"\"\"
    Принимает входящий вебхук лида от CRM.
    Добавляет тяжелую задачу анализа в асинхронный фон и мгновенно возвращает ответ 202 Accepted.
    \"\"\"
    if not payload.lead_id or not payload.raw_text:
        raise HTTPException(status_code=400, detail="Invalid payload: missing lead_id or raw_text")
        
    logger.info(f"[WEBHOOK RECEIVED] Получен лид {payload.lead_id} от {payload.email}")
    
    # Добавляем выполнение цепочки агентов в фоновые задачи FastAPI
    background_tasks.add_task(
        process_lead_pipeline_async, 
        payload.lead_id, 
        payload.email, 
        payload.raw_text
    )
    
    return {
        "status": "accepted",
        "message": "Lead registered successfully. Processing started in background.",
        "lead_id": payload.lead_id
    }
```

Этот код представляет собой надежную промышленную архитектуру: он мгновенно отдает ответ вебхуку, предотвращая таймауты со стороны CRM ( HubSpot/Salesforce), и безопасно обрабатывает тяжелые нейросетевые запросы в асинхронном фоне.

---

## Блок 8: Проектирование ИИ-отдела компании — оркестрация нескольких взаимодействующих LangGraph-графов

Когда вы автоматизируете процессы в крупных компаниях, вы быстро сталкиваетесь с тем, что один агент не может качественно закрывать все разнородные задачи. Попытка собрать "одного супер-агента" приводит к размытию системного промпта, перегрузке контекстного окна инструментами и резкому росту ошибок.

Профессиональный подход — построение **мультиграфовой архитектуры** (Multi-Graph Architecture), моделирующей реальную структуру компании:

```ascii
========================================================================================
ENTERPRISE MULTI-GRAPH ORCHESTRATION (ИИ-ОТДЕЛ КОМПАНИИ)
========================================================================================

                 Входящий запрос пользователя (Через API/Telegram)
                                    |
                                    v
                       +-------------------------+
                       |  ORCHESTRATOR / ROUTER  |
                       +-------------------------+
                         /          |          \\
       [ Запрос поддержки ]   [ Запрос цены ]   [ Запрос отчета ]
                       v            v            v
             +--------------+ +--------------+ +--------------+
             | SUPPORT GRAPH| | SALES GRAPH  | |ANALYTICS GRPH|
             | (RAG, Tickets)| |(Billing,CRM) | |(Data Metrics)|
             +--------------+ +--------------+ +--------------+
                       \\            |            /
                        v           v           v
                       [ Единая шина событий / Event Bus ]
========================================================================================
```

### Компоненты мультиграфовой архитектуры
- **Orchestrator Graph (Маршрутизатор):**
  Первичная легковесная языковая модель принимает запрос пользователя, определяет его глобальное намерение (intent) и перенаправляет сессию в соответствующий специализированный граф.
- **Support Graph (Отдел поддержки):**
  Оптимизирован для RAG-поиска по регламентам, интеграции с базой знаний и ответов на частые вопросы.
- **Sales Graph (Отдел продаж):**
  Содержит инструменты интеграции с CRM, выставления счетов, расчета скидок и обогащения контактных данных.
- **Analytics Graph (Отдел аналитики):**
  Отвечает за сбор метрик использования системы, отслеживание бюджетов на API-токены и формирование отчетов для руководства.

Все графы общаются между собой асинхронно через единую шину событий, передавая типизированные сообщения, что изолирует кодовую базу отделов и гарантирует высочайшую отказоустойчивость.

---

## Блок 9: Оптимизация бюджетов за счет агрессивного кэширования промптов (Prompt Caching)

При масштабном использовании ИИ-агентов в бизнесе расходы на API-токены могут составить тысячи долларов в месяц. Каждый раз, когда агент RAG делает запрос к модели, он отправляет огромный объем системных инструкций, описаний инструментов и извлеченных документов.

Современные API (Anthropic Claude 3.5 Sonnet, DeepSeek V3/R1) предоставляют мощную технологию оптимизации — **Prompt Caching** (Кэширование промптов):

- **Принцип работы:**
  Если статическая часть вашего промпта (системные инструкции, регламенты базы знаний RAG, JSON-схемы инструментов) превышает 1024 токена, провайдер кэширует ее векторное представление в оперативной памяти своих серверов.
- **Экономия:**
  При повторных вызовах в рамках того же часа за токены, попавшие в кэш (Prompt Cache Hits), вы платите **на 90% меньше** (например, $0.30 за миллион токенов вместо $3.00 у Anthropic). Более того, скорость генерации ответа (TTFT - Time to First Token) снижается в 2-3 раза.

### Правила проектирования промптов для кэширования
Вы обязаны структурировать промпт так, чтобы статическая часть (правила, регламенты, схемы) всегда находилась **в самом начале запроса**, а динамическая часть (текущий вопрос пользователя, изменчивые метаданные) — в самом конце. Любое изменение в начале промпта инвалидирует весь кэш, поэтому история диалога должна добавляться строго после статического блока инструкций.

---

## Блок 10: Нагрузочное стресс-тестирование, предотвращение бесконечных циклов и финальная защита проектов

Перед передачей готовой автоматизации клиенту в промышленную эксплуатацию вы обязаны провести комплекс приемо-сдаточных испытаний (UAT) и стресс-тестирование системы:

1. **Тестирование лимитов (Rate Limits Stress Test):**
   Запустите симуляцию одновременного обращения 50-100 пользователей к вашим агентам. Убедитесь, что инфраструктура FastAPI выдерживает нагрузку, а на уровне интеграции с API LLM корректно отрабатывают лимиты (Error 429) с использованием асинхронных ретраев с экспоненциальной задержкой (Exponential Backoff).
   
2. **Защита от бесконечных циклов рассуждений (Doom Loops):**
   Проверьте, что во всех скомпилированных графах LangGraph жестко задан параметр `recursion_limit` (рекомендуемое значение — 15). Запустите тест, где агент принудительно получает некорректные ответы от инструментов, и убедитесь, что при достижении лимита рекурсии граф безопасно завершает работу и отправляет алерт администратору, а не зацикливается, сжигая бюджет.
   
3. **Финальная защита выпускных проектов:**
   Презентация вашей готовой сквозной архитектуры, демонстрация Loom-видео, передача репозиториев кодов, развертывание на серверах клиента и торжественное вручение дипломов курса AI Automation Architect!
"""

    slides = """# Презентация: Выпускной Капстон-проект и Карьерный старт

---
📊 Слайд 1. Упаковка и ценностное позиционирование
- Тема: Как перестать продавать часы разработки и начать продавать бизнес-результаты.
- Макет: Сравнение Hourly Billing и Value-Based Pricing.
- Ключевые моменты: Отказ от технических терминов, расчет окупаемости для клиента, упаковка готовых решений.

---
📊 Слайд 2. Старт на фрилансе: Отклики на Upwork
- Тема: Как писать коммерческие предложения, которые закрывают сделки.
- Макет: Чек-лист идеального отклика (The Hook, Case studies, CTA).
- Ключевые моменты: Анализ боли клиента в первых строчках, демонстрация релевантных кейсов, Zoom-CTA.

---
📊 Слайд 3. Видео-портфолио на Loom
- Тема: Продажа сложных ИИ-решений через визуальные демонстрации.
- Макет: 3-шаговый фреймворк видео: Проблема -> Архитектура под капотом -> Результат.
- Ключевые моменты: Длина до 3 минут, демонстрация визуального холста n8n, демонстрация ценности.

---
📊 Слайд 4. Предпроектный аудит бизнес-процессов
- Тема: Защита проекта от расползания рамок (Scope Creep).
- Макет: Схема аудита "As-Is" и "To-Be".
- Ключевые моменты: Платный аудит, картирование узких мест, расчет объемов данных и бюджетов на API.

---
📊 Слайд 5. Договоры автоматизации и SLA
- Тема: Юридическая защита AI-инженера.
- Макет: Важнейшие юридические формулировки.
- Ключевые моменты: Ограничение ответственности за галлюцинации ИИ, SLA сторонних API (OpenAI/n8n).

---
📊 Слайд 6. Масштабирование и запуск ИИ-агентства
- Тема: Рост от фрилансера-соло в полноценное ИИ-агентство.
- Макет: Организационная структура агентства.
- Ключевые моменты: Наем джуниоров, создание собственных повторно используемых модулей (IP Assets).

---
📊 Слайд 7. Асинхронная архитектура на Python
- Тема: Разработка ядра оркестрации на FastAPI.
- Макет: Фрагменты кода асинхронного FastAPI-вебхука с Background Tasks.
- Ключевые моменты: Использование asyncio, фоновые фоновые задачи, логирование выполнения.

---
📊 Слайд 8. Проектирование ИИ-отдела компании
- Тема: Оркестрация мультиграфовых систем.
- Макет: Схема взаимодействия графов поддержки, продаж и аналитики через Event Bus.
- Ключевые моменты: Изоляция ответственности, обмен сообщениями, распределенные сессии.

---
📊 Слайд 9. Prompt Caching и оптимизация токенов
- Тема: Сокращение расходов на API ИИ до 90%.
- Макет: Сравнение цен обычных запросов и кэшированных контекстов.
- Ключевые моменты: Кэширование системных инструкций и баз знаний в Claude/DeepSeek, структура запросов.

---
📊 Слайд 10. Стресс-тестирование и финальная сдача
- Тема: Проверка надежности системы перед продакшном.
- Макет: Чек-лист приемки проекта.
- Ключевые моменты: Тестирование лимитов (Rate Limits), защита от бесконечных циклов (Doom Loops), передача доков.

"""

    video = """# Сценарий видео: Выпускной Капстон-проект и Карьерный старт

| Кадр / Scene | Звуковое сопровождение / Voiceover |
| :--- | :--- |
| **Кадр 1: Спикер крупным планом, на заднем плане диплом курса.** | Поздравляю вас с выходом на финишную прямую — 26-ю, выпускную неделю нашего курса! Сегодня мы упакуем все полученные знания в готовый бизнес, защитим дипломные проекты и запустим вашу карьеру. |
| **Кадр 2: Графики сравнения Hourly vs Value-based pricing.** | Мы начнем с упаковки ваших услуг. Вы узнаете, как перестать продавать дешевые часы разработки и начать продавать ценность для бизнеса, повышая стоимость одного контракта до десятков тысяч долларов. |
| **Кадр 3: Интерфейс Upwork и разбор успешного Proposal.** | Мы разберем пошаговый алгоритм откликов на Upwork и Habr Freelance. Я покажу вам шаблоны откликов, которые моментально вовлекают клиентов и закрывают их на созвон. |
| **Кадр 4: Пример Loom видео с демонстрацией холста n8n.** | Вы научитесь записывать убойные Loom-видеопортфолио. Мы разберем формулу «Проблема - Схема под капотом - Итоговый результат», которая продает ваши решения за вас. |
| **Кадр 5: Интерактивный чек-лист предпроектного аудита.** | Мы изучим методологию проведения платного аудита бизнес-процессов компании. Это защитит ваши проекты от срыва сроков и расползания рамок задач. |
| **Кадр 6: Текст юридического договора с выделенными пунктами о галлюцинациях.** | Мы разберем юридические аспекты. Вы получите готовые шаблоны договоров на автоматизацию с жестким разграничением ответственности за галлюцинации ИИ и падения внешних API. |
| **Кадр 7: Схема структуры ИИ-агентства.** | Мы обсудим стратегию масштабирования: как вырасти из фрилансера-соло в полноценное ИИ-агентство, нанимая младших разработчиков и перепродавая готовые модули. |
| **Кадр 8: Код асинхронного FastAPI сервера на Python.** | В практической части мы разберем асинхронную программную архитектуру на FastAPI для фоновой обработки вебхуков и координации агентов. |
| **Кадр 9: Мультиграфовая архитектура LangGraph.** | Мы спроектируем ИИ-отдел компании, настроив бесконфликтное взаимодействие нескольких независимых LangGraph-графов через общую шину событий. |
| **Кадр 10: IDE с примером Prompt Caching в Claude API.** | Мы внедрим Prompt Caching для экономии бюджета на токены до 90% и проведем стресс-тестирование всей системы на устойчивость к высоким нагрузкам. Вперед, к защите дипломов! |
"""

    return lecture, slides, video


def create_week_26_en():
    title = "Capstone Project and Career Launch"
    
    lecture = """# Week 26. """ + title + """

Welcome to Week 26 of our professional curriculum for AI Automation Architects! Behind you lie 6 months of intense training: you have journeyed from understanding REST API mechanics and crafting visual workflows in n8n to async programming in Python, building complex multi-agent state machines in LangGraph, and implementing production-grade reliability frameworks (Harness Engineering).

This final capstone week is entirely dedicated to preparing and presenting your Capstone Project, as well as launching your career as a elite, high-income specialist in the global market. We will dismantle freelancer client acquisition strategies, learn how to run paid process audits, draft legally sound contracts, package highly visual Loom portfolios, and scale your personal practice into a structured AI agency yielding up to an 80% profit margin.

---

## Block 1: Service Packaging — Positioning & Value-Based Pricing

Most developers make a critical mistake when entering the B2B market: they try to sell raw technology. They write in their proposals: *"I build n8n workflows, write Python scripts, and maintain Qdrant vector databases."* To a business owner, this sounds like gibberish. Businesses do not buy technology. They buy solutions to their pain: cutting operational costs, increasing lead processing speeds, reducing support overheads, or boosting sales conversion rates.

### The Positioning Shift
You must position yourself not as a "tech freelancer" but as an **AI Automation Architect** — a strategic partner who transforms company operations with AI.
- *Incorrect:* *"I will build you an n8n support chatbot for $500."*
- *Correct:* *"I will implement an AI-powered customer inquiry handler with a 98% accuracy rate. This will enable you to reduce support staff from 10 to 2 representatives, saving $6,000 monthly while dropping response times to 2 seconds."*

### Value-Based Pricing Methodology
If you bill by the hour, you cap your income by the hours in a day. Even worse, the faster and more skilled you become, the less you get paid for a task.
You must transition to a **Value-Based Pricing** model:
1. Conduct an audit and calculate the annual economic savings (ROI) of your solution.
2. If your system saves the client $72,000 a year, the value is massive.
3. You can confidently price your project at 20-30% of those annual savings — for example, $18,000. For the client, this is a highly profitable deal that fully pays for itself within just 3 months, yielding pure profit thereafter.

```ascii
========================================================================================
VALUE-BASED PRICING MODEL vs HOURLY BILLING
========================================================================================

 [ HOURLY BILLING ]:    50 hours of work * $50/hour = $2,500 (Client bargains over hours)
 
 [ VALUE-BASED ]:       Business value: $72,000/year saved.
                        Project price: $18,000 (Client pays for ROI and payback)
========================================================================================
```

---

## Block 2: Freelance Start — Crafting Winning Upwork and Freelance Proposals

Securing high-paying contracts on freelance platforms (like Upwork or Habr Freelance) requires strict adherence to proposal writing rules. Generic, ChatGPT-generated proposals like *"Hello, I am an experienced AI developer, ready to do your project"* are instantly archived by clients.

### The Winning Proposal Framework

- **The Hook (First 2 Lines):**
  This is the only text preview a client sees in their dashboard without clicking open the full proposal. You must capture their attention immediately. Instead of introducing yourself, demonstrate deep insight into their problem: *"I read your description about syncing leads from Typeform to HubSpot via n8n. You will almost certainly encounter contact duplication issues if users submit the form multiple times — here is how I resolve this at the architectural level..."*
  
- **Actionable Solution Draft:**
  Briefly outline the high-level architecture of your solution (3-4 sentences). Show that you already have a plan to execute the project.
  
- **Social Proof:**
  Attach links to 1-2 of your past relevant case studies showing concrete business results and metrics.
  
- **Zoom Call CTA:**
  End with a clear invitation to chat: *"Let's hop on a brief 10-minute Zoom call tomorrow. I can show you a live demo of a similar HubSpot sync engine I built for a previous client. What time works best for you?"*

---

## Block 3: Loom Portfolio — Recording Effective 3-Minute Video Demos

B2B clients rarely understand raw GitHub code and do not believe plain text portfolios. The absolute best sales tool for an AI architect is a library of **Loom video walkthroughs** of your functioning systems. These are short (2-3 minute) screencasts where you show your systems in action.

### The Loom Video Framework
1. **The Hook (The Problem) — first 15 seconds:**
   *"In this video, I will show you how we automated the sales department for a logistics company, dropping lead qualification times from 2 hours to 45 seconds."*
2. **How It Works (Under the Hood) — 60 seconds:**
   Open your n8n canvas or LangGraph state machine. Walk the client through the key nodes in plain English: *"Here, a webhook catches the lead, this AI node parses the intent, and this module searches our database."* This proves your deep engineering expertise.
3. **The Magic (The Outcome) — 45 seconds:**
   Show the "magic" live. Submit a test lead and show how, within 10 seconds, an enriched CRM card is created with a perfect, AI-generated email draft ready to send.

Having 5-6 Loom videos in your portfolio addresses 80% of client objections before they even book a call.

---

## Block 4: Process Audit — Designing Comprehensive Discovery Checklists

Beginning any automation build without performing a thorough pre-project audit is a guaranteed path to failure. Clients often do not understand their own workflows clearly. Your job is to bring structural order.

You must execute a **paid Discovery Audit** using this checklist:

- [ ] **Process Mapping (As-Is):**
  Step-by-step documentation of the current manual workflow. Who copies what line, where, and when?
- [ ] **Identifying Bottlenecks:**
  Where does manual repetition slow down operations? Where do leads fall out due to slow response times?
- [ ] **Data & API Inventory:**
  What APIs are available for the client's CRM, ERP, messaging apps, and databases? Is documentation accessible?
- [ ] **Financial Calculations:**
  How many leads/transactions flow through the company per month? How many hours are spent per lead? This is required to compute ROI and predict future API token costs.

Following the audit, you deliver a comprehensive "To-Be" architecture blueprint, establishing the exact Scope of Work.

---

## Block 5: AI Automation Contracts — Legal SLA and Liability Management

Deploying AI systems in business involves unique legal risks. LLMs can hallucinate (output false claims as facts), APIs can experience downtime, and working with customer data requires strict privacy compliance. Your B2B contracts must safeguard you from these liabilities.

### Critical AI Automation Clauses

1. **Probabilistic Output Disclaimer:**
   The contract must clearly state that LLM outputs are products of probabilistic modeling. The developer is not liable for occasional hallucinations, inaccuracies, or output errors, provided the system passes the agreed-upon acceptance tests.
2. **Third-Party API Outage Exemptions (SLA):**
   Explicitly exempt yourself from liability for outages, latency spikes, or price hikes on OpenAI, Anthropic, Qdrant, n8n Cloud, or HubSpot servers.
3. **Data Privacy & Compliance:**
   Ensure the client agrees not to pass highly sensitive personal data (e.g., social security numbers) through the AI pipeline, and guarantee that you configure APIs to opt-out of model training.

---

## Block 6: Agency Launch — Scaling from a Freelancer to an Agency

Once you hit a personal capacity limit earning $5,000–$10,000 per month, scaling requires transitioning to an **agency model**:

- **Delegating Routine (Junior Engineers):**
  Hire junior developers. Delegate routine tasks: basic n8n canvas assembly, writing simple Python scraper scripts, and frontend styling. Your time must be focused strictly on system architecture, sales, and client relationship management.
- **Building IP Assets:**
  Instead of designing every single project from scratch, build universal, reusable internal templates (RAG structures, CRM-to-Telegram alerting modules). This enables you to deploy client projects in a few days instead of a month, pushing project margins to 80-90%.

---

## Block 7 (Python Development): Developing Async FastAPI Agent Orchestrators

For your capstone project, you must demonstrate a deep understanding of professional asynchronous Python. We build a high-performance web service using **FastAPI** that coordinates incoming webhooks from CRMs and triggers a non-blocking background agent analysis pipeline.

```python
import asyncio
from fastapi import FastAPI, BackgroundTasks, HTTPException
from pydantic import BaseModel, EmailStr
import httpx
import logging

# Configure production logging
logging.basicConfig(level=logging.INFO, format="%(asctime)s - %(levelname)s - %(message)s")
logger = logging.getLogger("CapstoneOrchestrator")

app = FastAPI(
    title="Capstone AI Agent Orchestrator",
    description="High-performance async core for enterprise AI agent coordination",
    version="1.0.0"
)

class LeadWebhook(BaseModel):
    lead_id: str
    email: str
    raw_text: str

async def process_lead_pipeline_async(lead_id: str, email: str, raw_text: str):
    \"\"\"
    Asynchronous lead analysis pipeline.
    Executes semantic enrichment, ICP validation, and CRM syncing in background.
    \"\"\"
    logger.info(f"[PIPELINE START] Processing started for lead {lead_id}")
    
    # Simulate async LLM call for intent summary (2 seconds)
    await asyncio.sleep(2)
    logger.info(f"[AGENT 1] Summarized intent for {lead_id}")
    
    # Simulate async RAG search (3 seconds)
    await asyncio.sleep(3)
    logger.info(f"[AGENT 2] Grounded answer retrieved for {lead_id}")
    
    # In production, you would perform async POST requests via httpx:
    # async with httpx.AsyncClient() as client:
    #     await client.post("https://crm.client.com/api/leads", json=...)
    
    logger.info(f"[PIPELINE SUCCESS] Lead {lead_id} successfully synced to CRM. Task finished.")

@app.post("/api/v1/leads", status_code=202)
async def receive_lead_webhook(payload: LeadWebhook, background_tasks: BackgroundTasks):
    \"\"\"
    Receives CRM webhook and immediately returns 202 Accepted.
    Executes the heavy multi-agent pipeline in the background thread.
    \"\"\"
    if not payload.lead_id or not payload.raw_text:
        raise HTTPException(status_code=400, detail="Invalid payload: missing lead_id or raw_text")
        
    logger.info(f"[WEBHOOK RECEIVED] Received lead {payload.lead_id} from {payload.email}")
    
    # Add heavy pipeline task to FastAPI background worker thread
    background_tasks.add_task(
        process_lead_pipeline_async, 
        payload.lead_id, 
        payload.email, 
        payload.raw_text
    )
    
    return {
        "status": "accepted",
        "message": "Lead registered successfully. Processing started in background.",
        "lead_id": payload.lead_id
    }
```

This code demonstrates robust enterprise design: it returns a response to the webhook immediately, preventing timeouts on CRM servers, and processes intensive AI tasks in the background.

---

## Block 8: Enterprise Multi-Graph Architectures in LangGraph

When automating workflows for large enterprises, a single agent quickly becomes a bottleneck. Trying to build a "universal agent" results in prompt dilution, context window bloating, and high error rates.

A professional approach utilizes a **Multi-Graph Architecture**, modeling a company's departmental structure:

```ascii
========================================================================================
ENTERPRISE MULTI-GRAPH ORCHESTRATION (COMPANY AI DEPARTMENT)
========================================================================================

                 Inbound User Request (API/Telegram/Email)
                                    |
                                    v
                       +-------------------------+
                       |  ORCHESTRATOR / ROUTER  |
                       +-------------------------+
                         /          |          \\
       [ Support Request ]   [ Billing Request ]   [ Report Request ]
                       v            v            v
             +--------------+ +--------------+ +--------------+
             | SUPPORT GRAPH| | SALES GRAPH  | |ANALYTICS GRPH|
             | (RAG, Tickets)| |(Billing,CRM) | |(Data Metrics)|
             +--------------+ +--------------+ +--------------+
                       \\            |            /
                        v           v           v
                       [ Centralized Message Event Bus ]
========================================================================================
```

### Multi-Graph Components
- **Orchestrator Graph (Router):**
  A lightweight router model determines the user's intent and forwards the session to the designated sub-graph.
- **Support Graph:**
  Optimized for RAG knowledge retrievals, ticketing systems, and customer support.
- **Sales Graph:**
  Equipped with CRM integrations, invoice calculators, and sales pipelines.
- **Analytics Graph:**
  Monitors usage metrics, compiles summaries, and tracks token expenditures.

---

## Block 9: Budget Optimization via Prompt Caching

During high-volume enterprise deployments, API token expenditures can quickly scale. Modern APIs (Anthropic Claude 3.5 Sonnet, DeepSeek V3/R1) provide a powerful cost optimization tool — **Prompt Caching**:

- **How It Works:**
  If the static prefix of your prompt (system rules, RAG knowledge bases, tool JSON schemas) exceeds 1024 tokens, the provider caches its vector representation in memory.
- **Cost Savings:**
  Subsequent queries in the same hour that hit the cached prefix are charged **up to 90% less** (e.g., $0.30 per million tokens instead of $3.00 at Anthropic). Latency (TTFT) also drops by 2-3x.

### Prompt Structuring Rules for Caching
Always structure prompts so that the static context (rules, schemas, static RAG documents) sits at the **very beginning of the prompt**, while dynamic variables (user query, conversation history) sit at the very end.

---

## Block 10: Load Testing, Recursion Prevention, and Project Handover

Before delivering the system to your client for production release, perform a rigorous Quality Assurance checklist:

1. **Load Testing (Rate Limits):**
   Simulate 50-100 concurrent users accessing your agents. Verify FastAPI stability and ensure that LLM API calls utilize asynchronous exponential backoff retries on rate limit (HTTP 429) errors.
   
2. **Doom Loop Prevention:**
   Verify that all compiled LangGraph workflows have an explicit `recursion_limit` set (recommended value: 15) to prevent infinite loops and runaway API bills.
   
3. **Final Project Handover:**
   Present your E2E architecture, deliver documentation, hand over code repositories and API credentials, and celebrate your graduation as an elite AI Automation Architect!
"""

    slides = """# Slides: Capstone Project and Career Launch

---
📊 Slide 1. Service Packaging & Value-Based Pricing
- Title: Transitioning from charging hourly to selling business outcomes.
- Layout: Side-by-side comparison of Hourly Billing vs Value-Based Pricing.
- Key Points: Eliminating technical jargon, calculating financial payback for clients, packaging productized offers.

---
📊 Slide 2. Freelance Start: Upwork Proposals
- Title: Writing proposals that win contracts.
- Layout: Step-by-step proposal checklist (Hook, Case studies, CTA).
- Key Points: Addressing client pain points in the first sentence, providing video proof, Zoom-CTA.

---
📊 Slide 3. Loom Video Portfolios
- Title: Showcasing complex AI integrations visually.
- Layout: 3-step video structure: Problem -> Under the Hood -> Business Outcome.
- Key Points: Keep videos under 3 minutes, show the visual n8n canvas, focus on value generated.

---
📊 Slide 4. Discovery Audits & Process Mapping
- Title: Preventing scope creep via paid discovery phases.
- Layout: Process flow diagrams comparing As-Is and To-Be models.
- Key Points: Paid consulting, mapping process bottlenecks, tracking data sources and API token budgets.

---
📊 Slide 5. AI Contracts & SLA Limits
- Title: Legal frameworks for AI Automation Architects.
- Layout: Key contract clauses highlighting liability exemptions.
- Key Points: Probabilistic output disclaimers, third-party API outage exemptions (OpenAI/n8n).

---
📊 Slide 6. Scaling into an AI Agency
- Title: Scaling from a solo freelancer to a boutique studio.
- Layout: Corporate hierarchy showing developer and sales roles.
- Key Points: Hiring junior developers, building internal intellectual property (IP Assets).

---
📊 Slide 7. Asynchronous Python Backend
- Title: Developing the orchestrator engine in FastAPI.
- Layout: Code blocks showing FastAPI background tasks and async hooks.
- Key Points: Asynchronous processing, non-blocking background workers, production logging.

---
📊 Slide 8. Multi-Graph Architectures
- Title: Coordinating multi-agent networks in LangGraph.
- Layout: Multi-agent interaction flow via an Event Bus.
- Key Points: Separating support, sales, and analytics graphs, state synchronization, distributed sessions.

---
📊 Slide 9. Budget Optimization via Prompt Caching
- Title: Cutting API token expenditures by up to 90%.
- Layout: Cost analysis comparing standard and cached tokens.
- Key Points: Caching system rules and RAG context (Claude/DeepSeek), structuring prompt prefixes.

---
📊 Slide 10. Load Testing & Handover Checklists
- Title: Final delivery and system stress-testing.
- Layout: Final deployment inspection list.
- Key Points: Rate limit load tests, recursion limit safety (Doom loops), documentation packaging, final handover.

"""

    video = """# Video Script: Capstone Project and Career Launch

| Scene | Voiceover |
| :--- | :--- |
| **Scene 1: Close-up of speaker, course certificate in background.** | Welcome to the final, 26th week of our course! Today, we will pack all of your acquired knowledge into a functioning business, finalize capstones, and launch your career. |
| **Scene 2: Hourly vs Value-based pricing models compared.** | We will start with packaging your services. You will learn how to move away from billing by the hour and transition to value-based pricing, boosting your contract values. |
| **Scene 3: Screen showing an Upwork proposal checklist.** | We'll explore Upwork and Habr Freelance strategies. I will show you proposal templates designed to hook clients instantly and secure introductory Zoom calls. |
| **Scene 4: Demonstration of Loom video showing n8n.** | You'll learn how to record high-converting Loom video portfolios, showcasing your visual n8n canvases and highlighting the exact business value created. |
| **Scene 5: Interactive Process Audit checklist.** | We will cover paid Discovery phases, learning how to audit client business processes to prevent scope creep. |
| **Scene 6: AI Automation contract template.** | We'll discuss the legal side of things, outlining contract templates with strict clauses exempting you from LLM hallucinations and third-party API outages. |
| **Scene 7: Organizational structure of an AI Agency.** | We will map out your scaling strategy: moving from a solo freelancer to an agency by hiring juniors and developing reusable IP templates. |
| **Scene 8: FastAPI server Python code.** | In our Python development lab, we'll build an asynchronous FastAPI backend to handle incoming webhooks and execute non-blocking agent chains. |
| **Scene 9: Multiple LangGraph sub-graphs interacting.** | We will design enterprise-grade multi-graph structures, coordinating independent LangGraph workflows via a centralized event bus. |
| **Scene 10: IDE displaying prompt caching setup.** | Finally, we'll configure Prompt Caching to slash API costs by up to 90% and stress-test the entire system for production release. Let's begin! |
"""

    return lecture, slides, video


def main():
    print("Writing high-density files for Week 17 and Week 26 (RU and EN)...")
    
    # Paths
    base_path = "/Users/andreizelenov/Documents/Projects/AI learning/src/data/lessons"
    
    weeks = {
        17: {
            "ru": create_week_17_ru(),
            "en": create_week_17_en()
        },
        26: {
            "ru": create_week_26_ru(),
            "en": create_week_26_en()
        }
    }
    
    for week_num, langs in weeks.items():
        for lang, files in langs.items():
            lecture, slides, video = files
            dir_path = os.path.join(base_path, lang, f"week-{week_num}")
            os.makedirs(dir_path, exist_ok=True)
            
            # Write lecture.md
            with open(os.path.join(dir_path, "lecture.md"), "w", encoding="utf-8") as f:
                f.write(lecture)
                
            # Write slides.md
            with open(os.path.join(dir_path, "slides.md"), "w", encoding="utf-8") as f:
                f.write(slides)
                
            # Write video.md
            with open(os.path.join(dir_path, "video.md"), "w", encoding="utf-8") as f:
                f.write(video)
                
            print(f"✓ Generated week-{week_num} ({lang.upper()}) -> Size: {len(lecture)} bytes")

if __name__ == "__main__":
    main()
