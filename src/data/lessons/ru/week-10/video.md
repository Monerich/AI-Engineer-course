# Сценарий видео: OpenAI & Anthropic SDK: Structured Outputs

| Кадр / Scene | Звуковое сопровождение / Voiceover |
|:--- |:--- |
| **Крупный план спикера (Speaker close-up).** Появляется заголовок: *Неделя 10: OpenAI & Anthropic SDK – Structured Outputs*. | Добро пожаловать на десятую неделю. Сегодня мы фокусируемся на проектировании REST JSON-схем. Чтобы интегрировать стохастические LLM с внешними бизнес-системами, нам необходимы строгие и предсказуемые контракты данных, гарантирующие детерминированный вывод. |
| **Запись экрана (Screen recording).** Дерево вложенного JSON-объекта плавно форматируется и подсвечивается синтаксисом. | Во втором блоке мы разберем валидацию многоуровневых структур данных. Преобразование сырого текста в строгие вложенные JSON-объекты критически важно для минимизации галлюцинаций и безопасной передачи данных дальше по пайплайну. |
| **Анимация (Motion graphic).** Красный алерт `HTTP 429 Too Many Requests` перехватывается блоком `try/except` и превращается в таймер ожидания. | Третий блок посвящен обработке ошибок API. Сеть неизбежно дает сбои в продакшне. Мы научимся программно перехватывать сетевые исключения и анализировать заголовки rate limit, чтобы предотвращать фатальные падения наших скриптов. |
| **Интерфейс IDE (Code IDE view).** Настройка клиента Anthropic SDK и подгрузка файла `` в системный промпт. | В четвертом блоке мы перейдем к конфигурации официальных SDK OpenAI и Anthropic. Вы узнаете, как использовать системные инструкции и паттерны вроде для жесткого задания роли агента и архитектурных границ до начала генерации. |
| **Средний план спикера (Speaker medium shot).** Рядом появляются ползунки параметров: `Temperature`, `Top_P`, `Max_Tokens`. | Пятый блок охватывает параметры генерации. Мы научимся динамически управлять параметром `temperature` для контроля фактичности или креативности, а также настраивать `top_p` и `max_tokens` для удержания контекста в рамках бюджета. |
| **Инфографика (Infographic).** График экспоненциально растущей задержки (Exponential Backoff) с добавлением случайного шума (Jitter). | В шестом блоке мы внедряем стратегии восстановления после лимитов. Использование алгоритмов экспоненциальной задержки позволяет нашим пайплайнам плавно обходить ошибки 429 и продолжать работу без участия человека. |
| **Интерфейс IDE (Code IDE view).** Подсветка декораторов `@field_validator` и `@model_validator` в классе Pydantic. | Седьмой блок вводит Pydantic V2 для Structured Outputs. Мы будем использовать кастомные валидаторы для внедрения сложной сквозной бизнес-логики прямо на этапе парсинга и типизации ответов модели. |
| **Анимация (Circular flow animation).** Ошибка валидации возвращается в модель вместе со стеком ошибки, после чего модель выдает исправленный JSON. | Восьмой блок — это цикл самовосстановления JSON. Опираясь на концепцию диагностической петли, мы заставим агента читать собственные ошибки валидации Pydantic и исправлять их, закрывая разрыв верификации (Verification Gap). |
| **Запись экрана (Screen recording).** Сохранение шагов агента в таблицу SQLite в виде типизированных объектов. | В девятом блоке мы реализуем надежное исполнение (Durable Execution). Вместо логов в виде сырых строк, мы будем сохранять историю рассуждений агента в базу данных как строго типизированные классы, позволяя возобновить работу после любого сбоя. |
| **Крупный план спикера (Speaker close-up).** Схема эволюции базы данных: `Schema V1 -> Schema V2`. | Наконец, десятый блок посвящен версионированию схем. Мы освоим паттерн интеллектуального дозаполнения (Semantic Upfilling), при котором LLM автономно извлекает недостающие поля из старых документов при миграции на новые форматы данных. |

Готовы ли вы двигаться дальше и создать какой-либо учебный артефакт на основе этого плана, или у вас есть вопросы по конкретному блоку?