# Сценарий видео: Case Study: Local CLI Agent with Tools

| Кадр / Scene | Звуковое сопровождение / Voiceover |
|:--- |:--- |
| **Scene 1.** Speaker close-up, then screen shows a diagram of an LLM connecting to web_search, read_file, and write_file tools. | Welcome to Week 12. Today, we are building a production-grade local CLI agent from scratch. In Block 1, we assemble a Unified CLI Toolkit containing web search APIs like Tavily, and file utilities like read and write. Carefully describing these tools acts as strict instructions, ensuring the model selects them without hallucinating. |
| **Scene 2.** Animation of a Python script saving a JSON array into a local SQLite database. | Block 2 is all about Session Logging. We must treat the agent like a brilliant engineer with amnesia. We implement durable execution by persisting message histories and state in a SQLite database after each step. This ensures we can recover our raw history and user configurations if the process crashes. |
| **Scene 3.** Terminal window displaying color-coded ReAct loops (Thought vs. Action). | Block 3 introduces Step Trace Logs. Making the agent runtime observable is a core rule, because without it, evaluations turn into subjective judgments. We beautifully format the execution steps, visually separating the agent's internal thoughts from the raw tool outputs. |
| **Scene 4.** Split screen: A user saves a file in an IDE, and a background Python script instantly detects the change. | In Block 4, we transition from reactive to ambient agents. We write Python directory listeners that operate in the background to monitor local file changes. We utilize debounce patterns to filter out system noise and trigger the agent only when necessary. |
| **Scene 5.** The CLI agent finishes a task, and a `` file automatically updates on screen. | Block 5 covers Auto-Documentation. A core harness principle dictates that a clean handoff must occur at the end of each session. We implement an automated reporting sequence to summarize task outputs and update external memory files like ``. |
| **Scene 6.** Screen recording of VS Code showing a Python project structure with a `` file. | Block 6 is our main practice session. We consolidate our work into a fully functional CLI agent that uses Tavily search and filesystem access. We assemble a custom, 1500-line Python mini-harness that effectively bridges system prompts and our tool registry. |
| **Scene 7.** Terminal UI transforms from basic gray text to a rich layout with a loading spinner and syntax-highlighted JSON. | Block 7 focuses on Python Development by integrating the `rich` terminal library. We upgrade our basic console logs into premium visual layouts. We implement dynamic loading spinners for long tasks and render JSON arguments with clear syntax highlighting. |
| **Scene 8.** A diagram showing an LLM outputting `<thought>` tags being filtered before hitting the user interface. | In Block 8, we hide the model's internal reasoning. Complex tasks require models to think step by step before generating an answer. We use XML `<thought>` tags and a streaming parser to keep this internal draft hidden from the user, rendering only the final, polished response. |
| **Scene 9.** A context window bar fills up to 85%, then compresses into a single summary block. | Block 9 tackles Context Compaction. To prevent API rate limit crashes, we summarize older conversation blocks when our context window hits 85 percent capacity. We also use the filesystem offload pattern to save massive tool payloads directly to disk, keeping the active window clear. |
| **Scene 10.** A simulated crash in the terminal, followed by the command `agent --resume run_123` restoring the exact state. | Finally, Block 10 handles CLI Session recovery. Durable execution is non-negotiable for any agent running longer than 60 seconds. We engineer our harness to load state from SQLite and automatically restart by run ID, ensuring the agent picks up exactly where it left off. |

Would you like to move forward with generating the code snippets that accompany these video scenes, or should we prepare the student assessment quiz next?