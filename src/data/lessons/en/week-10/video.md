# Сценарий видео: OpenAI & Anthropic SDKs: Structured Outputs

| Кадр / Scene | Звуковое сопровождение / Voiceover |
|:--- |:--- |
| **Scene 1: Speaker close-up.**<br>Graphic appears: `{ "status": "active" }` | Welcome to Week 10. Today, we conquer Structured Outputs. Prompting an LLM for a strict JSON format forces the model to create predictable structures and actively limits hallucinations. This is how we turn a text generator into a deterministic API. |
| **Scene 2: Screen recording.**<br>Visualizing a nested JSON payload mapping to a dashboard. | Next, we handle Data Validation. Using structured output parsers, we can extract complex, multi-layered data—like a story's title alongside an array of characters and scenes. Defining clear templates in your prompt guarantees your downstream systems can read the data. |
| **Scene 3: B-roll.**<br>Terminal window showing a Python `try/except` block catching an HTTP 400 error. | In production, APIs will fail. You must anticipate network crashes using standard Python exception handling. Crucially, when an error occurs, your harness must provide actionable instructions to the agent on how to fix the broken payload. |
| **Scene 4: Code snippet.**<br>Initializing the OpenAI client and injecting a System Prompt. | To connect our code, we configure the SDKs. We initialize our clients securely using environment variables. Then, we inject strict system instructions to define the agent's exact role and behavioral constraints before it processes any user data. |
| **Scene 5: Infographic.**<br>Sliders for Temperature, Top-P, and Token Limits. | Control your output via Generation Parameters. Temperature controls randomness; Top-P restricts token sampling. Furthermore, you must define strict token ceilings to prevent runaway costs, though setting it too low can result in truncated, invalid JSON. |
| **Scene 6: Architecture diagram.**<br>Nodes showing API requests hitting a "429 Too Many Requests" wall, then entering a retry queue. | At scale, you will hit rate limits. Pounding an API with sequential requests will trigger 429 errors. True resiliency requires batching your network requests and implementing deliberate execution pauses to optimize usage and stay within quotas. |
| **Scene 7: Python code.**<br>Pydantic classes with `@field_validator` decorators. | We enforce these boundaries in Python using Pydantic V2. By deploying custom `@field_validator` and `@model_validator` decorators, we programmatically verify that the LLM's output is logically and mathematically sound before our application accepts it. |
| **Scene 8: Flowchart.**<br>JSON generation -> Pydantic Error -> Error fed back to LLM -> Corrected JSON. | When validation fails, the system shouldn't crash. We build self-healing JSON loops. The harness traps the Pydantic error, appends the specific trace to the message history, and forces the agent to read its own mistake and rewrite the payload. |
| **Scene 9: Database schema.**<br>SQLite tables storing `run_id`, `thought`, `action`, and `observation`. | To make the runtime observable, we type and save every step of the ReAct loop. By committing session traces to an SQLite database, we achieve "durable resume"—allowing an agent to instantly recover its context if the network drops. |
| **Scene 10: Speaker close-up.**<br>Graphic: 'V1 Schema -> Migration Script -> V2 Schema'. | Finally, as your production agent evolves, its schemas will change. We implement Just-In-Time data migrations to deterministically map legacy JSON traces into current structures, preventing schema-induced amnesia and ensuring long-term stability. |

Does this script format give you a solid foundation for recording the video modules? We can dive into writing the assessment quizzes for this week next, if you're ready.