# Сценарий видео: n8n AI Nodes and LangChain Integration

| Кадр / Scene | Звуковое сопровождение / Voiceover |
|:--- |:--- |
| **Scene 1:** Speaker close-up, transitioning to a dark-mode n8n canvas. A tree diagram highlights a root "AI Agent" node connecting to various sub-nodes. | Welcome to Week 6, where we explore how n8n's Advanced AI nodes are built directly on top of the LangChain framework. This modular architecture allows root nodes to connect with sub-nodes like models and memory, letting you visually assemble complex cognitive pipelines. |
| **Scene 2:** Screen recording of the n8n canvas. The cursor selects the "AI Agent" node, configures it to "Conversational Agent", and adjusts the system message. | Let's examine the AI Agent node operating in Conversational Agent mode. The agent autonomously manages the dialogue and decides when to request external data based on the identity and rules you define in its system prompt [4-6]. |
| **Scene 3:** Split screen showing an n8n Custom Code node on the left and an IDE with TypeScript class code on the right. | When standard options fall short, you can engineer Advanced n8n Tools using Custom Code or HTTP Request nodes. For community nodes, this involves writing custom TypeScript logic via an execute method, safely handling authentication through n8n's Credentials vault. |
| **Scene 4:** A visual comparison graphic: a RAM chip labeled "Window Buffer" fading away, next to a durable PostgreSQL database cylinder labeled "Persistent Memory". | Bot memory is critical for persistent conversations. While Window Buffer Memory stores context temporarily in RAM, enterprise agents require durable external databases like Postgres Chat Memory to completely eliminate LLM amnesia between sessions. |
| **Scene 5:** Icons of a PDF, CSV, and TXT file flow into a "Default Data Loader" node, which outputs a standardized JSON Document block. | To prepare files for vectorization, we utilize Document Loaders. The Default Data Loader ingests binary data from PDFs, CSVs, and TXT files, extracting the text and metadata to construct unified LangChain Document objects. |
| **Scene 6:** An animation of a long document being sliced by scissors into smaller, overlapping chunks. | Feeding massive documents to an LLM causes instruction bloat and context loss. We solve this using Text Splitters—like the Recursive Character Splitter—to mathematically chunk the data with calculated overlap, preserving semantic meaning across boundaries. |
| **Scene 7:** A Python code snippet appears, highlighting the `dotenv` import and an Anthropic SDK instantiation with `try/except` timeout parameters. | Advanced developers eventually drop down to Python to utilize the official OpenAI and Anthropic SDKs natively. We secure API keys using the dot-env library and implement strict try/except blocks to manage client timeouts and network errors gracefully. |
| **Scene 8:** An n8n Tool node interface highlighting the "Description" field and a strict JSON schema featuring an `enum` parameter list. | Agents determine which tool to use by reading Tool JSON schemas injected into their prompt. By engineering highly descriptive prompts and enforcing strict parameter types like enums, you guarantee deterministic tool selection. |
| **Scene 9:** A red warning shield deflects a malicious input prompt, followed by a diagram of a `PreToolUse` hook analyzing a JSON request. | However, autonomous tools open the door to dangerous prompt injection attacks. To shield your architecture, you must deploy input sanitization middleware and PreToolUse hooks to intercept and validate every tool argument before the irreversible action is executed. |
| **Scene 10:** The LangSmith dashboard is shown, expanding a nested OpenTelemetry trace tree to reveal millisecond latency and token counts. | Finally, we implement OpenTelemetry tracing to monitor our agents' reasoning processes. By connecting platforms like LangSmith, we capture every token, latency metric, and tool payload, giving us the diagnostic visibility needed to debug complex multi-tool execution chains. |

Are you ready to move forward to generating the presentation or teleprompter script for Week 7, or would you like to review any of these LangChain concepts further?