# Сценарий видео: Custom ReAct Agent from Scratch in Python

| Кадр / Scene | Звуковое сопровождение / Voiceover |
|:--- |:--- |
| **Scene 1: Speaker close-up.**<br>Graphic: Code showing a Python dictionary acting as a Tool Registry. | Welcome to Week 11. Today, we are building a custom ReAct agent from scratch in Python. We start with the Tool Registry, an architecture that explicitly maps string names generated by the LLM to callable Python functions. This decouples the orchestrator from execution, allowing your agent to dynamically discover its capabilities without hardcoded logic. |
| **Scene 2: Screen recording.**<br>Terminal showing an agent pulling real-time data using Tavily/SerpAPI. | Large Language Models are limited by their training data cutoffs, so we must integrate Search APIs like Tavily and SerpAPI,. By equipping the agent with these tools, we enable it to autonomously scrape web results and pull real-time facts into its context window for complex research tasks,. |
| **Scene 3: B-roll.**<br>Visual of an agent writing a Markdown report to a local folder with a padlock icon. | Next, we build File Action tools, such as `read_file` and `write_file`, giving the agent hands to interact with the local workspace. However, we must implement strict directory bounds to prevent the agent from escaping its sandbox and modifying sensitive host system files,. |
| **Scene 4: Code snippet.**<br>FastAPI Swagger UI showing micro-endpoints for local Python tools. | To scale our capabilities, we expose our Python tools as REST APIs via FastAPI micro-endpoints. By wrapping local scripts into network-accessible services, we standardize the Agent-Computer Interface, meaning your tools can seamlessly be called by external platforms like n8n. |
| **Scene 5: Split screen.**<br>Left: Python docstring. Right: Auto-generated JSON Schema. | Writing JSON schemas manually is tedious and error-prone. Instead, we dynamically generate tool schemas directly from Python docstrings. The descriptions of the tools and parameters act as direct instructions to the model, ensuring the LLM always receives exact, up-to-date formatting requirements. |
| **Scene 6: Architecture diagram.**<br>Docker or E2B container isolating a Python script execution. | Because agents write and run code autonomously, we must enforce Runtime Isolation by executing scripts inside secure sandboxes or containers. This handles system dependencies cleanly and ensures your host infrastructure is protected from destructive commands or credential leaks. |
| **Scene 7: Python code.**<br>Highlighting a custom `@tool` decorator generating a schema. | We streamline our Python development by building custom `@tool` decorators,. Using reflection to inspect function signatures, this decorator automatically registers the function and generates the required schema on the fly, enforcing your repository as the single source of truth,. |
| **Scene 8: Flowchart.**<br>Circular loop diagram: Think -> Act -> Observe. | Now we build the brain: the canonical ReAct loop. This is essentially a deterministic `while True` loop wrapping the LLM,. The model outputs a "Thought" to reason about a problem, requests an "Action", and the harness returns an "Observation", continuing until the task is complete,. |
| **Scene 9: Data pipeline visual.**<br>Harness intercepting `tool_calls` and triggering a "Filesystem Offload" for massive HTML. | In the Action phase, our harness must capture `tool_calls`, execute the functions, and return a `tool_result`. For massive payloads over twenty thousand tokens, we implement a "Filesystem Offload" pattern to write the result to a text file, keeping the context window safe from token bloat. |
| **Scene 10: Speaker close-up.**<br>Graphic: Circuit breaker triggering to stop a Doom Loop. | Finally, because agents can suffer from myopia and fall into "doom loops" where they endlessly repeat broken approaches, we implement Safety Caps. We configure strict iteration limits, usually 10 to 15 loops, and Human-in-the-Loop interrupts to stop infinite execution and protect your API budget,,. |

Would you like me to use the `create_video_overview` tool to generate an actual video podcast based on this script for you?